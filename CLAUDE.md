# NLP 学习项目 — 辅导上下文

> 本文件由 Claude 自动读取。**每次新会话开始时，先读本文件 + `PROGRESS.md`，再决定今天教什么。**
> 本文件是学习者档案的"当前快照"，随进度持续更新。详细测评记录见 `PROGRESS.md`。

---

## 一、这个项目是什么

一套「以对话为主线、以文件夹资料为辅助」的 NLP 学习系统。
终极目标：具备在 NLP 前沿方向做研究、产出可投稿论文的能力。

**分工约定：**
- **对话是主线** —— 讲解、答疑、出题、批改、调节奏都在对话里发生
- **文件夹是辅助** —— 讲义、代码骨架、练习、档案。不追求大而全，按需生成
- **测评驱动** —— 每模块结束前检测，按结果调整深度与节奏，不按固定进度表推

---

## 二、学习者档案

| 项目 | 情况 |
|---|---|
| 身份 | 学生 |
| 每周投入 | 20 小时以上 |
| 数学基础 | 线性代数 + 微积分较熟 |
| 英文 | 能读英文论文但慢，需要辅助 → **阶段 1、2 走中文主线**，阶段 3 起逐步过渡到英文原文 |
| 目标 | 掌握到阶段 3（现代 LLM 体系），最终做前沿研究、发论文 |
| 目标周期 | 约 12 个月到第一篇可投稿论文 |

### 硬件（两台机器）

| 机器 | 配置 | 定位 / 项目路径 |
|---|---|---|
| 台式机（主力） | 9700X 8C/16T + RTX 5070 Ti 16GB + **内存仅 16GB** | 阶段 2 的 GPT 预训练、阶段 3 的 LoRA/QLoRA 全在此。`G:\Project\nlp-learning` |
| 笔记本（备用） | RTX 3060 Laptop 6GB | 讲义、纸笔推导、NumPy 小实验。够跑阶段 1 全部 + 阶段 2 小规模验证。`D:\项目\nlp-learning` |

⚠️ **内存 16GB 与显存一样大 = 没有兜底空间。** 阶段 1、2 不受影响；
阶段 3 加载 7B 会内存 OOM，必须 `device_map` + `low_cpu_mem_usage=True`。**届时再讲。**
**完整环境明细见 `SETUP.md`；当前安装状态见第三节末。**

**跨设备约定：** 所有状态写进本仓库文件，用 Git 同步。对话历史不跨设备，但本文件 + `PROGRESS.md` 保证辅导可无缝接续。

---

## 三、当前进度（定长快照 —— 一律覆盖，不追加）

**阶段 1 — 深度学习地基**
**当前模块：M2 多层网络与训练技巧**（第二课「优化器」：**动量部分推导 + 实验全部收口**；两道收尾题已布置未收，RMSProp 未开始）
**最后更新：2026-09-21**

### 进度与掌握度

| 模块 | 状态 | 掌握度 | 详细记录 |
|---|---|---|---|
| L0 入门摸底 | ✅ 2026-09-06 | C+ | [`progress/L0.md`](progress/L0.md) |
| M1 反向传播与计算图 | ✅ 2026-09-07 | **B** | [`progress/M1.md`](progress/M1.md) |
| M2 多层网络与训练技巧 | 🔄 进行中 | — | [`progress/M2.md`](progress/M2.md) |

### 能力画像

| 维度 | 评级 | 一句话依据 |
|---|---|---|
| 数学推导 | **B+** | 反向传播、He 初始化、动量级数、两项递推均独立推出；09-18~20 **特征方程 →  \(\lvert z\rvert = \sqrt{\beta}\) → κ 上限 → β\* 全链独立完成**，且经实验验证。**写完不自检**（E10） |
| 代码实现 | **B−** | 能把推导翻译成向量化 numpy；**照公式抄代码会抄错且不自检** |
| 概念理解深度 | **A−** | 自己推出了动量的完整机制；独立判断出「动量只是缓解」「β 太大反而慢」的两个原因。但仍倾向**复述现象**而非给机制 |
| 工程调试 | **B−** | 09-10 首次无提示设计对照实验；09-20 **看懂了「坏停止判据能把比较关系搞反」**（我的 71 步假象）。仍不主动拿理论核对读数 |
| 论文阅读 / 研究品味 | 未涉及 | — |

### ⚠️ 教他的要诀（最重要的一节，讲解前先看这里）

1. **不要重讲概念，他懂 —— 要逼他动笔。** 他常说「推出来就是」，**要他写出来**。
2. **从他已拥有的结论做最小改动。** 实证：双重求和从头推 `Var(z')` 他没懂，「从你昨天推对的式子出发，只改最后一项」立刻通。
3. **代具体数字核对 —— 一问就通。** 实证：他把梯度当常数，我只问「w₁ 变成 0.5 时梯度还是 20 吗」，他立刻自己改对。
4. **拆步骤、留空行、标编号。** 信息密度过高比缺少排版更伤（他明确反馈过）。
5. **追问"所以呢？为什么？"** —— 他答完常停在复述现象，不能就此收下。
6. **要求先写目标形状/预测，再写式子/做实验。** 他习惯先凭手感写完再补。
7. 🌟 **信心问题 —— 本项目最重要的长期观察。** 模式：**正确的判断被他自己写成疑问句。**
   09-09、09-10 各出现过，09-10 当面点破（「判断力够了，缺的是敢当结论交出来」）。
   ⚠️ **但 09-18 我误诊了一次**：他写「是不是就是…」，实际原因是**记不清 λ 和 lr 的含义**，不是不敢认。
   **→ 点之前先问一句「哪里不确定」。** 是信心问题就当场点；是记号遗忘就指他去查 `NOTATION.md`。
8. 💡 **好信号：他会主动质疑、追问。顺着他的追问讲，效率远高于我单方面讲。**
   实证：09-18 **他抓到我把 `1/(1−β)=10` 和 `√κ=10` 两个巧合并排写成了因果**。
9. ⚠️ **我自己的记号不一致，是他卡住的头号原因 —— 先怀疑自己再怀疑他。**
   **→ 讲任何带符号的内容前先 Read [`NOTATION.md`](NOTATION.md)，引入新符号先写进去。**
   实证：E9 是我一口气引入 g、λ、新下标却都没定义。**一次只改一处；表里标了冲突的符号必须当场点明。**

### 当前薄弱点（按优先级）

1. 🔺 **不主动自检（最顽固）** —— 判据/预测已经在手里却不拿去核对：
   漏看 `E[Z1²]=964`；09-15 自检点**预先**给了还是没跑（E10）。09-18 **问了就做对，但仍不是自发的**
2. **numpy 惯用法** —— 写过 `if Z1 > 0:` 这种标量式分支，尚未建立"对数组写表达式"的直觉
3. **工具链生疏** —— 曾把 Python 粘进 PowerShell、不熟 `np.sum` 的 axis。纯经验缺口，不单独补课
4. 无 PyTorch 经验、无工程调试经验（留到 M3）
5. **记号会忘**（09-18 忘了 λ、lr 含义）→ 指他查 `NOTATION.md`。书写约定 `dL/dw`（不写 `L/w`）

### ⏭ 下一步：收两道已布置的题 → E6/E8 复查 → RMSProp

**M2 动量部分推导 + 实验已全部收口**（`β*=((√κ−1)/(√κ+1))²`；实测最优 β=0.68 vs 理论 0.6694，
加速 8.24× vs √κ=10）。⚠️ 过程中**推翻了我 09-13 报的「71 步 / 4.9 倍」**（坏停止判据造成的假象，
真值 129 步），已如实告诉他。

**① 开场先收两道题** —— 题面、答案、收口要点**全在 `progress/M2.md` 09-20（续）第三节，开口前先读它**：
任务 1 解出 lr 的可行区间为空（**不是 lr 挑小了，是根本无解**，要和他推的 κ 上限接起来）；
任务 2 实根区改用大根 → 预测 106 步 vs 实测 107（**理论没错，是我套错了公式**）。

**② 考 E6 / E8**（复查期 09-20 已到，**连跳两次会话了，这次别再跳**）：
E6 训练时去掉 `1/m` 的后果；E8 反向传播相对数值求导的复杂度量级差在哪、为什么。

**③ RMSProp 三题**（09-13 布置至今未做，题目见 `progress/M2.md`）：
`s_t=βs_{t−1}+(1−β)g_t²`，`w←w−lr·g/√s` → 问 `g/√s≈±1` → **κ 还重要吗**。
⚠️ 先说清 s **带** `(1−β)` 而动量 v **不带**（`NOTATION.md` 已登记）。

**再往后：** Adam（β 拆成 β₁β₂）→ 学习率调度 → 回头打「batch 越大越好」
→ `optim_compare.py`（待建，按规则 4 留 TODO，**笔记本已可跑**）。

### 环境状态（2026-09-21｜细节见 `SETUP.md`）

| | 笔记本 | 台式机 |
|---|---|---|
| Python | **3.13.15** `D:\Program\Python\Python313` | **3.13.15** `G:\Program\Python\Python313`（系统默认仍是 3.14.3 `D:\PYTHON`） |
| 项目 venv | ✅ numpy 2.5.3/scipy/matplotlib/sklearn | ✅ 同样的包（09-21 实测，已重建在 3.13 上） |
| torch | 未装（M3 再说） | 未装 |

- 🆕 **笔记本 09-20 装好了 Python** → **2D 实验和 NumPy 练习笔记本就能跑，不必等台式机**
- 台式机：PowerShell 执行策略 Restricted → 用完整路径 `G:\Project\nlp-learning\.venv\Scripts\python.exe`
- ✅ **两台机器都已装 `dotclaude/` 到 `~/.claude/`**（台式机 09-21 完成，合并非覆盖，备份 `CLAUDE.md.bak-20260921`）
- 🆕 **两台机器现在都是 Python 3.13** —— 阶段 3 不必再另建 3.12 环境了，3.13 的 wheel 覆盖足够

## 四、辅导规则（重要 — 请严格遵守）

1. **先测后教。** 进入任何新模块前，先用 2–3 个问题探边界，确认真实起点。不要假设学习者"应该已经会了"。

2. **不给结论，给推导路径。** 他数学基础扎实，要给完整推导而不是"记住这个公式"。目标是他能在白纸上从损失函数推到每一个梯度。

3. **每个概念配一道能暴露理解深度的题。** 好题的标准：会背公式的人做不出来，真懂的人能做出来。避免纯计算题和纯定义题。

4. **代码骨架留空。** 核心部分（反向传播、注意力计算）留 `TODO` 让他填，并**同时给验证脚本**（如数值梯度校验），让他能自己判断对错，不依赖我批改。

5. **验收标准必须可执行。** 「过关」= 一个具体可运行的成果（如"不查资料用 NumPy 写两层 MLP，MNIST 95%"），不是"理解了 XX 概念"。

6. **答错时不要直接给答案。** 先判断错误类型（概念错 / 计算错 / 表述错），针对性追问，让他自己找到错处。只有连续两次卡住才给提示。

7. **每次测评后更新档案。** 把结果写进 `PROGRESS.md`，并同步更新本文件第三节"当前进度"。错题进错题本，错题在 1–2 周后要复现考察。

8. **英文过渡。** 阶段 1、2 用中文材料。从阶段 2 后期开始，每周至少带读一篇英文论文的摘要 + 方法部分，逐步提速。不要一次性切换。

9. **算力匹配。** 设计实验时先确认在哪台机器上跑。笔记本（6GB）上的任务规模要压缩；需要 16GB 的任务标注"台式机执行"。

10. **保持诚实。** 学习者水平不够时直说，不要为了鼓励而放水。研究能力的建立经不起自我欺骗。

11. **公式写法 —— 全文在全局 `~/.claude/CLAUDE.md` 和 [`NOTATION.md`](NOTATION.md) 第五节。**

    这里只留**最常被我自己违反**的一条：

    ⚠️ **`\(` 前面必须是空格或行首。** 2026-09-21 我在一次回复里违反 4 次，
    全部发生在**半角逗号和顿号之后**（写成 `\(\beta\),\(\lambda\)`），他当场反馈渲染失败。
    **→ 写完一段先扫一遍所有 `\(`，逐个检查前一个字符是不是空格。**

    其余：`$$...$$` 独立成行；裸拉丁变量包 `\mathit{}`；**表格单元格里不许出现裸竖线**；
    反引号只包代码标识符；终端版 `$$` 不渲染 → 改代码块包纯文本。

    **多步推导拆开排版、留空行、标步骤号。信息密度过高比缺少排版更伤。**

12. **档案必须保持定长 —— 每次写档案前先想清楚写进哪个文件。**

    2026-09-09 重构过一次（20KB+37KB ≈ **开场 28000 tokens**，才第 4 天）。根因：把「只增不减的详细批改」和「应被覆盖的当前状态」混在了一起。

    **写入规则：**

    | 内容 | 去哪 | 方式 |
    |---|---|---|
    | 某次会话的详细批改、他的原话、实验数据 | `progress/M*.md` | **追加** |
    | 进度表、错题本、能力画像、里程碑、会话日志 | `PROGRESS.md` | **覆盖** |
    | 当前模块、能力评级、教他的要诀、薄弱点、下一步、环境 | `CLAUDE.md` 第三节 | **覆盖** |

    - **详细记录绝不写进 `CLAUDE.md` 或 `PROGRESS.md`** —— 那两个文件每次会话都要加载。
    - 模块结业时，把该模块在 `CLAUDE.md` 第三节里的段落**压成表格里的一行**，详情已在 `progress/` 里。
    - `PROGRESS.md` 的会话日志**只留最近 15 行**，更早的每月压成一行。
    - **软上限：`CLAUDE.md` < 15KB，`PROGRESS.md` < 10KB。** 超了就该压缩，不是该新建小节。
    - 「教他的要诀」那节是**合并**不是追加 —— 新教训要么并进已有条目，要么替换掉过时的。

---

## 五、目录结构

```
nlp-learning/
├── CLAUDE.md            ← 本文件：辅导规则 + 当前快照（每次会话自动加载，必须定长）
├── NOTATION.md          ← ⚠️ 数学符号单一事实来源。讲带符号的内容前必读
├── PROGRESS.md          ← 进度表、错题本、能力画像、会话日志（定长，覆盖式更新）
├── progress/            ← 详细批改记录，按模块拆分。平时不加载，需要回溯时才读
│   ├── L0.md
│   ├── M1.md
│   └── M2.md
├── README.md            ← 总路线图与使用说明
├── SETUP.md             ← 两台机器的环境搭建
├── syllabus/            ← 阶段 1–3 的详细大纲与验收标准
├── assessments/         ← 测评题
├── 00-foundations/      ← 阶段 1：讲义 + 代码练习
├── 01-transformer/      ← 阶段 2
├── 02-llm/              ← 阶段 3
├── 03-research/         ← 阶段 4：论文笔记、复现
├── notes/               ← 他自己的笔记
└── data/                ← 数据集（不入 Git）
```

---

## 六、会话协议（关于档案何时被读取 — 重要）

`CLAUDE.md` 在**会话启动时加载一次**，之后不会自动重读。这带来三条必须遵守的规则：

### 对学习者（完整版见 `README.md`）

1. **换设备后开新会话，别恢复旧对话** —— 旧对话装的是它启动那刻的档案。**档案才是记忆。**
2. **开始：** `git pull` → 打开 Claude Code → 说「**继续学习**」
3. **结束：** 说「**更新档案**」→ 写完后 `git add -A && git commit && git push`。**没 push 就等于没学。**
4. 自己手动改过档案 → 说一声「档案我改过了」。

### 对 Claude

- 听到「**继续学习**」时：**① 先 `git status -sb` 确认没落后远端**（落后就 `git pull --ff-only`）；
  **② 再显式 Read** `CLAUDE.md` 和 `PROGRESS.md`，不要依赖启动时的自动加载。
  ⚠️ 2026-09-21 踩过：台式机落后 9 个提交，我用 09-13 的旧档案教了半场 ——
  让他重推早已清掉的 E9、还引用了他已复核推翻的实测数据。**Read 旧文件读得再认真也没用。**
- 听到「**更新档案**」时，**按规则 12 分流**：
  详细批改 → `progress/M*.md`（追加）；错题本/能力画像/会话日志 → `PROGRESS.md`（覆盖）；
  当前快照 → `CLAUDE.md` 第三节（覆盖）。写完提醒学习者 commit + push。
- 在同一会话内每次修改档案后，**告诉学习者需要 push**，不要默默改完就算了。
