# NLP 学习项目 — 辅导上下文

> 本文件由 Claude 自动读取。**每次新会话开始时，先读本文件 + `PROGRESS.md`，再决定今天教什么。**
> 本文件是学习者档案的"当前快照"，随进度持续更新。详细测评记录见 `PROGRESS.md`。

---

## 一、这个项目是什么

一套「以对话为主线、以文件夹资料为辅助」的 NLP 学习系统。
终极目标：具备在 NLP 前沿方向做研究、产出可投稿论文的能力。

**分工约定：**
- **对话是主线** —— 讲解、答疑、出题、批改、调整节奏，都在对话里发生。
- **文件夹是辅助** —— 存放讲义、代码骨架、练习、测评题、进度档案。资料不追求大而全，按需生成。
- **测评驱动** —— 每个模块结束前做检测，根据结果调整后续深度和节奏，不按固定进度表推。

---

## 二、学习者档案

| 项目 | 情况 |
|---|---|
| 身份 | 学生 |
| 每周投入 | 20 小时以上 |
| 数学基础 | 线性代数 + 微积分较熟 |
| 英文 | 能读英文论文但慢，需要辅助 → **阶段 1、2 走中文主线**，阶段 3 起逐步过渡到英文原文 |
| 目标 | 掌握到阶段 3（现代 LLM 体系），最终做前沿研究、发论文 |
| 目标周期 | 约 12 个月到第一篇可投稿论文 |

### 硬件（两台机器）

| 机器 | 配置 | 定位 |
|---|---|---|
| 台式机（主力） | 9700X (8C/16T) + RTX 5070 Ti 16GB + **内存 16GB** | 阶段 2 的 GPT 预训练、阶段 3 的 LoRA/QLoRA 微调，全部在此进行 |
| 笔记本（备用） | RTX 3060 Laptop (6GB) | 讲义阅读、NumPy/小规模实验、写代码。6GB 足够跑阶段 1 全部内容和阶段 2 的小规模验证 |

**台式机实测配置（2026-09-07 亲自探测）：**

| 项 | 实际值 |
|---|---|
| CPU | AMD Ryzen 7 9700X，8 核 16 线程 |
| 系统内存 | **15.2 GB 可用（16GB 条）** ← ⚠️ 这是全系统最紧的一环，见下方说明 |
| GPU | RTX 5070 Ti，显存 16303 MiB（约 15.9GB） |
| 驱动 | 610.62，CUDA UMD 13.3 → cu128 和 cu130 构建都能用 |
| OS | Windows 11 专业版 26200 |
| 磁盘 | C: 剩 440GB ／ D: 剩 352GB ／ **G: 剩 1.19TB（项目在此，数据集放这）** |
| Python | **3.14.3**，装在 `D:\PYTHON\python.exe`，系统级、无虚拟环境 |
| 已装包 | numpy 2.4.3 / scipy 1.17.1 / matplotlib 3.10.8 / pandas 3.0.1 / seaborn。**torch 未装** |

⚠️ **系统内存 16GB 是真实瓶颈，不是小事：**
显存和内存一样大（都是 16GB），意味着**没有内存兜底空间**。具体影响：
- 阶段 1、2 完全不受影响（数据量小）
- 阶段 3 QLoRA 微调 7B 模型时，`transformers` 默认会先把权重加载到内存再搬去显存 —— 16GB 内存装 7B 的 fp16 权重（约 14GB）会直接 OOM。
  必须用 `device_map` + `low_cpu_mem_usage=True` 走分片流式加载，或者提前把权重转成 4bit 存盘。届时会专门讲。
- 加内存到 32GB 会显著降低阶段 3 的折腾成本。不急，到阶段 3 前决定即可。

**跨设备约定：** 所有状态写进本仓库文件，用 Git 同步。对话历史不跨设备，但本文件 + `PROGRESS.md` 保证辅导可以无缝接续。

---

## 三、当前进度（定长快照 —— 一律覆盖，不追加）

**阶段 1 — 深度学习地基**
**当前模块：M2 多层网络与训练技巧**（第一课「参数初始化」已完成含实验，下一步第二课）
**最后更新：2026-09-09**

### 进度与掌握度

| 模块 | 状态 | 掌握度 | 详细记录 |
|---|---|---|---|
| L0 入门摸底 | ✅ 2026-09-06 | C+ | [`progress/L0.md`](progress/L0.md) |
| M1 反向传播与计算图 | ✅ 2026-09-07 | **B** | [`progress/M1.md`](progress/M1.md) |
| M2 多层网络与训练技巧 | 🔄 进行中 | — | [`progress/M2.md`](progress/M2.md) |

### 能力画像

| 维度 | 评级 | 一句话依据 |
|---|---|---|
| 数学推导 | **B** | 向量化反向传播、He 初始化均独立推出并通过验证 |
| 代码实现 | **B−** | 能把推导翻译成向量化 numpy；**照公式抄代码会抄错且不自检** |
| 概念理解深度 | **B+** | 明显高于自述水平；但倾向**复述现象**而非给出机制 |
| 工程调试 | **C−** | 会读诊断表了，但仍需提醒才去核对理论与读数 |
| 论文阅读 / 研究品味 | 未涉及 | — |

### ⚠️ 教他的要诀（最重要的一节，讲解前先看这里）

1. **不要重讲概念，他懂 —— 要逼他动笔。** 停在"我说你听"就是无效的。
2. **从他已拥有的结论做最小改动，不要从零严谨重推。**
   实证：用双重求和从头推 `Var(z')=n·Var(w)·E[a²]` 他没懂；
   换成"从你昨天推对的式子出发，只有最后一项变了"立刻通。
3. **代具体数字核对。** 抽象式子他容易滑过去，代几个数就抓住了。
4. **拆步骤、留空行、标编号。** 信息密度过高比缺少排版更伤（他明确反馈过）。
5. **追问"所以呢？为什么？"** —— 他答完常停在复述现象，不能就此收下。
6. **要求先写目标形状/预测，再写式子/做实验。** 他习惯先凭手感写完再补，得被要求才用维度倒推法。
7. 🌟 **信心问题（持续观察）：他会写下推翻自己结论的理由，却仍交上被推翻的结论。**
   2026-09-09 预测大初始化会"训练慢"，同时自己质疑"损失函数不是把 sigmoid 问题抵掉了吗"——
   那句质疑才是对的。**已告诉他下次敢信。这是通往研究独立性的关键一步，要反复强化。**
8. 💡 **好信号：他会主动质疑中间结果、追问符号约定。顺着他的追问讲，效率远高于我单方面讲。**

### 当前薄弱点（按优先级）

1. **numpy 惯用法** —— 写过 `if Z1 > 0:` 这种标量式分支，尚未建立"对数组写表达式"的直觉
2. **不主动自检** —— 有理论预测却不拿去核对读数（漏看 `E[Z1²]=964` 这种明显异常）
3. **工具链生疏** —— 曾把 Python 粘进 PowerShell、不熟 `np.sum` 的 axis。纯经验缺口，不单独补课
4. 无 PyTorch 经验、无工程调试经验（留到 M3）
5. 记号：仍写 `L/z` 而非 `∂L/∂z`；答题不按提问顺序列

### ⏭ 下一步：M2 第二课

初始化管"训练开始时"，第二课管"训练过程中" —— 即他用 `lr=5.0` 一步踩死网络的那个坑。
路线：学习率 → 动量 → RMSProp → **Adam** → 学习率调度。大纲见 `syllabus/stage1-foundations.md`。
**按规则 1，开讲前先探边界**（已知：Dropout、Adam 都没听说过）。

### 环境状态（2026-09-09）

- 笔记本（RTX 3060 6GB）：**未装 Python**
- 台式机：**Python 3.14.3**（`D:\PYTHON`）；项目内已建 `.venv`，装了 numpy/scipy/matplotlib/scikit-learn
  - ⏳ torch 未装。M1、M2 不需要，**到 M3 再装**
  - ⚠️ 3.14 较激进。PyTorch 官方源 cu130 有 2.14.0、cu128 有 2.11.0 可用于 3.14，装 torch 没问题；
    但阶段 3 的 `bitsandbytes` / `flash-attn` 对 3.14 的 wheel 大概率滞后 → **进阶段 3 前另建 3.12 环境**
  - ⚠️ 5070 Ti 是 Blackwell（sm_120），**必须 CUDA 12.8+ 的构建**（驱动 610.62 支持到 13.3），详见 `SETUP.md`
- **PowerShell 执行策略为 Restricted**，`Activate.ps1` 会被拦
  → 约定用完整路径 `G:\Project\nlp-learning\.venv\Scripts\python.exe`，或在 VSCode 里选解释器
- Git：已初始化并推送到 GitHub 私有仓库

## 四、辅导规则（重要 — 请严格遵守）

1. **先测后教。** 进入任何新模块前，先用 2–3 个问题探边界，确认真实起点。不要假设学习者"应该已经会了"。

2. **不给结论，给推导路径。** 学习者数学基础扎实，讲解要给出完整推导，而不是"记住这个公式"。尤其是反向传播，必须让他自己能在白纸上从损失函数推到每一个梯度。

3. **每个概念配一道能暴露理解深度的题。** 好题的标准：会背公式的人做不出来，真懂的人能做出来。避免纯计算题和纯定义题。

4. **代码骨架留空。** 给练习代码时，核心部分（尤其是反向传播、注意力计算）留 `TODO` 让学习者自己填，并**同时提供验证脚本**（如数值梯度校验），让他能自己判断对错，不依赖我批改。

5. **验收标准必须可执行。** 每个模块的"过关"定义成一个具体可运行的成果（如"不查资料用 NumPy 写出两层 MLP 并在 MNIST 达到 95%"），不是"理解了 XX 概念"。

6. **答错时不要直接给答案。** 先判断错误类型（概念错 / 计算错 / 表述错），针对性追问，让他自己找到错处。只有连续两次卡住才给提示。

7. **每次测评后更新档案。** 把结果写进 `PROGRESS.md`，并同步更新本文件第三节"当前进度"。错题进错题本，错题在 1–2 周后要复现考察。

8. **英文过渡。** 阶段 1、2 用中文材料。从阶段 2 后期开始，每周至少带读一篇英文论文的摘要 + 方法部分，逐步提速。不要一次性切换。

9. **算力匹配。** 设计实验时先确认在哪台机器上跑。笔记本（6GB）上的任务规模要压缩；需要 16GB 的任务标注"台式机执行"。

10. **保持诚实。** 学习者水平不够时直说，不要为了鼓励而放水。研究能力的建立经不起自我欺骗。

11. **公式写法 —— 已实测定死（2026-09-09 桌面版逐项测过），只有两条：**

    - **重要公式 → `$$...$$` 独立成行**（唯一稳定可渲染的方式）
    - **行内符号 → 反引号包纯文本**：`Var(w)`、`E[a²]`、`dL/dz`、`ReLU(z)`、`z'`。
      希腊字母与上下标用 Unicode（σ ∂ Σ ² ᵀ ′），那是字符不是 LaTeX，哪里都能显示。

    ❌ **不要用 `\(...\)`** —— 它只在内容含 `\mathrm` 这类反斜杠命令时才渲染，
    纯文本内容（`\(n\)`、`\(z'\)`）会被当成转义括号显示成 `(n)`。而行内公式多数是单个变量，基本必挂。
    `$...$`、``$`...`$``、`\[...\]` 一律不渲染。

    ⚠️ 终端版里 `$$` 也不渲染 —— 学习者说他在终端、或反馈看到一堆 `\frac`，就把 `$$` 换成代码块包纯文本。

    **多步推导一律拆开排版、留空行、标步骤号。信息密度过高比缺少排版更伤**（他明确反馈过）。

12. **档案必须保持定长 —— 每次写档案前先想清楚写进哪个文件。**

    2026-09-09 重构过一次：当时 `CLAUDE.md` 20KB + `PROGRESS.md` 37KB ≈ **每次开场 28000 tokens**，
    而那才第 4 天、1 个模块。根因是把「只增不减的详细批改」和「应被覆盖的当前状态」混在了一起。

    **写入规则：**

    | 内容 | 去哪 | 方式 |
    |---|---|---|
    | 某次会话的详细批改、他的原话、实验数据 | `progress/M*.md` | **追加** |
    | 进度表、错题本、能力画像、里程碑、会话日志 | `PROGRESS.md` | **覆盖** |
    | 当前模块、能力评级、教他的要诀、薄弱点、下一步、环境 | `CLAUDE.md` 第三节 | **覆盖** |

    - **详细记录绝不写进 `CLAUDE.md` 或 `PROGRESS.md`** —— 那两个文件每次会话都要加载。
    - 模块结业时，把该模块在 `CLAUDE.md` 第三节里的段落**压成表格里的一行**，详情已在 `progress/` 里。
    - `PROGRESS.md` 的会话日志**只留最近 15 行**，更早的每月压成一行。
    - **软上限：`CLAUDE.md` < 15KB，`PROGRESS.md` < 10KB。** 超了就该压缩，不是该新建小节。
    - 「教他的要诀」那节是**合并**不是追加 —— 新教训要么并进已有条目，要么替换掉过时的。

---

## 五、目录结构

```
nlp-learning/
├── CLAUDE.md            ← 本文件：辅导规则 + 当前快照（每次会话自动加载，必须定长）
├── PROGRESS.md          ← 进度表、错题本、能力画像、会话日志（定长，覆盖式更新）
├── progress/            ← 详细批改记录，按模块拆分。平时不加载，需要回溯时才读
│   ├── L0.md
│   ├── M1.md
│   └── M2.md
├── README.md            ← 总路线图与使用说明
├── SETUP.md             ← 两台机器的环境搭建
├── syllabus/            ← 阶段 1–3 的详细大纲与验收标准
├── assessments/         ← 测评题（做完后答案与批改记录进 PROGRESS.md）
├── 00-foundations/      ← 阶段 1：讲义 + 代码练习
├── 01-transformer/      ← 阶段 2
├── 02-llm/              ← 阶段 3
├── 03-research/         ← 阶段 4：论文阅读笔记、复现
├── notes/               ← 学习者自己的笔记
└── data/                ← 数据集（不入 Git）
```

---

## 六、会话协议（关于档案何时被读取 — 重要）

`CLAUDE.md` 在**会话启动时加载一次**，之后不会自动重读。这带来三条必须遵守的规则：

### 对学习者

1. **换设备后不要恢复旧对话，开新会话。**
   旧对话的上下文里装的是它启动那一刻的档案。你在另一台机器上的进展它看不到，
   会导致 Claude 以为你还停在几天前的位置。
   对话历史本来就不跨设备 —— **档案才是记忆，对话只是当次的工作台**。

2. **每次开始学习的固定动作：**
   ```
   git pull
   ```
   然后在项目目录打开 Claude Code，说一句「**继续学习**」。

3. **每次结束学习的固定动作：** 说一句「**更新档案**」，等 Claude 写完档案后：
   ```
   git add -A && git commit -m "..." && git push
   ```
   没 push 就等于没学 —— 换台机器就丢了。

4. **如果你自己手动改了 `CLAUDE.md` 或 `PROGRESS.md`**，在对话里说一声「档案我改过了」，
   否则同一个会话内 Claude 看到的还是旧版本。

### 对 Claude

- 听到「**继续学习**」时，**显式用 Read 工具重读** `CLAUDE.md` 和 `PROGRESS.md`，
  不要依赖会话启动时的自动加载 —— 那份可能已经过期。
- 听到「**更新档案**」时，**按规则 12 分流**：
  详细批改 → `progress/M*.md`（追加）；错题本/能力画像/会话日志 → `PROGRESS.md`（覆盖）；
  当前快照 → `CLAUDE.md` 第三节（覆盖）。写完提醒学习者 commit + push。
- 在同一会话内每次修改档案后，**告诉学习者需要 push**，不要默默改完就算了。
