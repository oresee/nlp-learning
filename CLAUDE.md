# NLP 学习项目 — 辅导上下文

> 本文件由 Claude 自动读取。**每次新会话开始时，先读本文件 + `PROGRESS.md`，再决定今天教什么。**
> 本文件是学习者档案的"当前快照"，随进度持续更新。详细测评记录见 `PROGRESS.md`。

---

## 一、这个项目是什么

一套「以对话为主线、以文件夹资料为辅助」的 NLP 学习系统。
终极目标：具备在 NLP 前沿方向做研究、产出可投稿论文的能力。

**分工约定：**
- **对话是主线** —— 讲解、答疑、出题、批改、调节奏都在对话里发生
- **文件夹是辅助** —— 讲义、代码骨架、练习、档案。不追求大而全，按需生成
- **测评驱动** —— 每模块结束前检测，按结果调整深度与节奏，不按固定进度表推

---

## 二、学习者档案

| 项目 | 情况 |
|---|---|
| 身份 | 学生 |
| 每周投入 | 20 小时以上 |
| 数学基础 | 线性代数 + 微积分较熟 |
| 英文 | 能读英文论文但慢，需要辅助 → **阶段 1、2 走中文主线**，阶段 3 起逐步过渡到英文原文 |
| 目标 | 掌握到阶段 3（现代 LLM 体系），最终做前沿研究、发论文 |
| 目标周期 | 约 12 个月到第一篇可投稿论文 |

### 硬件（两台机器）

| 机器 | 配置 | 定位 / 项目路径 |
|---|---|---|
| 台式机（主力） | 9700X 8C/16T + RTX 5070 Ti 16GB + **内存仅 16GB** | 阶段 2 的 GPT 预训练、阶段 3 的 LoRA/QLoRA 全在此。`G:\Project\nlp-learning` |
| 笔记本（备用） | RTX 3060 Laptop 6GB | 讲义、纸笔推导、NumPy 小实验。够跑阶段 1 全部 + 阶段 2 小规模验证。`D:\项目\nlp-learning` |

⚠️ **内存 16GB 与显存一样大 = 没有兜底空间。** 阶段 1、2 不受影响；
阶段 3 加载 7B 会内存 OOM，必须 `device_map` + `low_cpu_mem_usage=True`。**届时再讲。**
**完整环境明细见 `SETUP.md`；当前安装状态见第三节末。**

**跨设备约定：** 所有状态写进本仓库文件，用 Git 同步。对话历史不跨设备，但本文件 + `PROGRESS.md` 保证辅导可无缝接续。

---

## 三、当前进度（定长快照 —— 一律覆盖，不追加）

**阶段 1 — 深度学习地基**
**当前模块：M2 多层网络与训练技巧**（第二课「优化器」：动量已讲完，E9 已清；**特征方程三步 + RMSProp 三题均已布置未收**）
**最后更新：2026-09-15**

### 进度与掌握度

| 模块 | 状态 | 掌握度 | 详细记录 |
|---|---|---|---|
| L0 入门摸底 | ✅ 2026-09-06 | C+ | [`progress/L0.md`](progress/L0.md) |
| M1 反向传播与计算图 | ✅ 2026-09-07 | **B** | [`progress/M1.md`](progress/M1.md) |
| M2 多层网络与训练技巧 | 🔄 进行中 | — | [`progress/M2.md`](progress/M2.md) |

### 能力画像

| 维度 | 评级 | 一句话依据 |
|---|---|---|
| 数学推导 | **B** | 反向传播、He 初始化、稳定性条件、动量两个级数、带动量的两项递推均独立推出。**写完不自检**（E10） |
| 代码实现 | **B−** | 能把推导翻译成向量化 numpy；**照公式抄代码会抄错且不自检** |
| 概念理解深度 | **A−** | 自己推出了动量的完整机制；独立判断出「动量只是缓解」「β 太大反而慢」的两个原因。但仍倾向**复述现象**而非给机制 |
| 工程调试 | **C+** | 09-10 首次无提示设计出正确对照实验；但仍不主动拿理论预测核对读数 |
| 论文阅读 / 研究品味 | 未涉及 | — |

### ⚠️ 教他的要诀（最重要的一节，讲解前先看这里）

1. **不要重讲概念，他懂 —— 要逼他动笔。** 停在"我说你听"就是无效的。
2. **从他已拥有的结论做最小改动，不要从零严谨重推。**
   实证：用双重求和从头推 `Var(z')=n·Var(w)·E[a²]` 他没懂；
   换成"从你昨天推对的式子出发，只有最后一项变了"立刻通。
3. **代具体数字核对 —— 一问就通，比讲道理快得多。**
   实证：09-10 他把梯度当常数，我不解释，只问「w₁ 变成 0.5 时 dL/dw₁ 还是 20 吗」，
   他算出 10，立刻自己把整道题改对。抽象式子他会滑过去，代数字就抓住了。
4. **拆步骤、留空行、标编号。** 信息密度过高比缺少排版更伤（他明确反馈过）。
5. **追问"所以呢？为什么？"** —— 他答完常停在复述现象，不能就此收下。
6. **要求先写目标形状/预测，再写式子/做实验。** 他习惯先凭手感写完再补，得被要求才用维度倒推法。
7. 🌟 **信心问题 —— 本项目最重要的长期观察。已第三次出现，09-10 当面点破。**
   模式：**正确的判断被他自己写成疑问句。**
   09-09 预测大初始化"训练慢"，旁边那句自我质疑才是对的；09-10 答对动量机制却写成「…吗？」。
   **已告诉他：判断力够了，缺的是敢当结论交出来。每次再出现都要当场点，不要放过。**
8. 💡 **好信号：他会主动质疑中间结果、追问符号约定。顺着他的追问讲，效率远高于我单方面讲。**
   实证：09-13 他问「什么叫最优 lr」，顺势讲出了稳定界、非单调性和「公平比较超参」这一整块。
9. ⚠️ **我自己的记号不一致，是他卡住的头号原因 —— 先怀疑自己再怀疑他。**
   **→ 已建 [`NOTATION.md`](NOTATION.md)：讲任何带符号的内容前先 Read 它，引入新符号必须先写进去。**
   实证：E9 卡掉一整次会话，查明是我一口气引入 g、λ、新下标却一个都没定义 ——
   他以为 g 是个新概念（其实只是 `dL/dw` 的缩写）。M1 的 `1/m` 约定也坑过一次。
   **一次只改一个地方；表里标了冲突的符号（z、β、λ、n）必须当场点明。**

### 当前薄弱点（按优先级）

1. 🔺 **不主动自检（最顽固，已三次）** —— 判据/预测已经在手里却不拿去核对：
   漏看 `E[Z1²]=964`；09-15 我把 β=0 的自检点**预先**给了他，他还是没跑就交了符号全反的答案（E10）
2. **numpy 惯用法** —— 写过 `if Z1 > 0:` 这种标量式分支，尚未建立"对数组写表达式"的直觉
3. **工具链生疏** —— 曾把 Python 粘进 PowerShell、不熟 `np.sum` 的 axis。纯经验缺口，不单独补课
4. 无 PyTorch 经验、无工程调试经验（留到 M3）
5. ~~记号迁移弱~~ —— **09-15 查实 E9 的根因是我没定义符号，不是他跟不上。已建 `NOTATION.md`。
   这条从他的薄弱点里划掉，转记到我的要诀 9。**
6. 记号：`∂` 难打，**已约定写 `dL/dw`**（`L/w` 字面是除法）。答题仍不按提问顺序列

### ⏭ 下一步：收特征方程三步，再讲 RMSProp

**开场第一件事：** 让他答「β=0 代进 `w_t=(1+β−lr·λ)w_{t−1}−βw_{t−2}` 会变成什么」。
上次他两个符号都写反，是我说「反了」他才改的 —— **要确认自检真跑了，不是被提示翻过去的**（E10）。

**已布置未收 ①：特征方程三步**（题面/答案/实测对照全在 `progress/M2.md` 2026-09-15 节）
设 `w_t = zᵗ` → `z² − (1+β−lr·λ)z + β = 0` → β=0 自检 → 算 `f(−1)=2+2β−lr·λ > 0`
→ 得 **`lr·λ < 2(1+β)`**。**这个上界上次是我甩给他的，这次要由他自己推出来。**

**已布置未收 ②：RMSProp 三题**（早已布置、完全没做，题目见 `progress/M2.md`）
`s_t = βs_{t−1} + (1−β)g_t²`，`w ← w − lr·g/√s` → 问 `g/√s ≈ ±1` → **κ 还重要吗**。
⚠️ 讲之前先说清 RMSProp 的 β **带** `(1−β)` 而动量**不带**（`NOTATION.md` 已登记此冲突）。

**再往后：** Adam（β 拆成 β₁β₂）→ 学习率调度 → 回头打「batch 越大越好」（mini-batch SGD）
→ 练习 `00-foundations/numpy_mlp/optim_compare.py`（**待建**，按规则 4 留 TODO，需台式机）。

**已答复的路线问题（09-15）：** RNN/LSTM **学**，约一周（撞 seq2seq 瓶颈 + Mamba/SSM 还活着）；
**CNN 不单独学**，只在 M6 吸收残差 / LN vs BN / 卷积=归纳偏置三个概念。理由见 `progress/M2.md`。

### 环境状态（2026-09-15，无变化）

- 笔记本（RTX 3060 6GB）：**未装 Python**。纸笔推导阶段完全够用
- 台式机：**Python 3.14.3**（`D:\PYTHON`），项目内已建 `.venv`（numpy/scipy/matplotlib/scikit-learn）
  - ⏳ **torch 未装**。M1、M2 不需要，到 M3 再装。**版本/CUDA/阶段 3 的 3.12 环境细节全在 `SETUP.md`**
  - **PowerShell 执行策略 Restricted**，`Activate.ps1` 会被拦 → 用完整路径
    `G:\Project\nlp-learning\.venv\Scripts\python.exe`，或在 VSCode 里选解释器
- Git：已推送到 GitHub 私有仓库

## 四、辅导规则（重要 — 请严格遵守）

1. **先测后教。** 进入任何新模块前，先用 2–3 个问题探边界，确认真实起点。不要假设学习者"应该已经会了"。

2. **不给结论，给推导路径。** 学习者数学基础扎实，讲解要给出完整推导，而不是"记住这个公式"。尤其是反向传播，必须让他自己能在白纸上从损失函数推到每一个梯度。

3. **每个概念配一道能暴露理解深度的题。** 好题的标准：会背公式的人做不出来，真懂的人能做出来。避免纯计算题和纯定义题。

4. **代码骨架留空。** 给练习代码时，核心部分（尤其是反向传播、注意力计算）留 `TODO` 让学习者自己填，并**同时提供验证脚本**（如数值梯度校验），让他能自己判断对错，不依赖我批改。

5. **验收标准必须可执行。** 每个模块的"过关"定义成一个具体可运行的成果（如"不查资料用 NumPy 写出两层 MLP 并在 MNIST 达到 95%"），不是"理解了 XX 概念"。

6. **答错时不要直接给答案。** 先判断错误类型（概念错 / 计算错 / 表述错），针对性追问，让他自己找到错处。只有连续两次卡住才给提示。

7. **每次测评后更新档案。** 把结果写进 `PROGRESS.md`，并同步更新本文件第三节"当前进度"。错题进错题本，错题在 1–2 周后要复现考察。

8. **英文过渡。** 阶段 1、2 用中文材料。从阶段 2 后期开始，每周至少带读一篇英文论文的摘要 + 方法部分，逐步提速。不要一次性切换。

9. **算力匹配。** 设计实验时先确认在哪台机器上跑。笔记本（6GB）上的任务规模要压缩；需要 16GB 的任务标注"台式机执行"。

10. **保持诚实。** 学习者水平不够时直说，不要为了鼓励而放水。研究能力的建立经不起自我欺骗。

11. **公式写法（09-15 他否掉了反引号方案，行内写法待定）：**

    - **重要公式 → `$$...$$` 独立成行**（唯一确定稳定的方式）
    - ❌ **反引号不再包数学** —— 他原话「B 很难受，反引号就给代码块比较好」。
      反引号只用于代码标识符：`np.sum`、`dW1`、`mlp.py`
    - ❌ `$...$` 不渲染（09-15 复测确认）
    - 🔄 **他要求行内用 `\(...\)`。** 09-09 的结论是它只在含反斜杠命令时才渲染
      （`\beta` 有效，裸 `\(w\)` 会挂），**尚未复测**。
      ⏳ **09-15 埋了 D1–D5 五项探针，下次开场先问结果，再把本条定死。**
    - **临时写法：** 含希腊字母/运算符的行内式用 `\(...\)`；单个裸拉丁字母（w、z、g）直接写

    ⚠️ 终端版里 `$$` 也不渲染 —— 他说在终端、或看到一堆 `\frac`，就改用代码块包纯文本。

    **多步推导一律拆开排版、留空行、标步骤号。信息密度过高比缺少排版更伤**（他明确反馈过）。

12. **档案必须保持定长 —— 每次写档案前先想清楚写进哪个文件。**

    2026-09-09 重构过一次（20KB + 37KB ≈ **开场 28000 tokens**，而那才第 4 天、1 个模块）。
    根因：把「只增不减的详细批改」和「应被覆盖的当前状态」混在了一起。

    **写入规则：**

    | 内容 | 去哪 | 方式 |
    |---|---|---|
    | 某次会话的详细批改、他的原话、实验数据 | `progress/M*.md` | **追加** |
    | 进度表、错题本、能力画像、里程碑、会话日志 | `PROGRESS.md` | **覆盖** |
    | 当前模块、能力评级、教他的要诀、薄弱点、下一步、环境 | `CLAUDE.md` 第三节 | **覆盖** |

    - **详细记录绝不写进 `CLAUDE.md` 或 `PROGRESS.md`** —— 那两个文件每次会话都要加载。
    - 模块结业时，把该模块在 `CLAUDE.md` 第三节里的段落**压成表格里的一行**，详情已在 `progress/` 里。
    - `PROGRESS.md` 的会话日志**只留最近 15 行**，更早的每月压成一行。
    - **软上限：`CLAUDE.md` < 15KB，`PROGRESS.md` < 10KB。** 超了就该压缩，不是该新建小节。
    - 「教他的要诀」那节是**合并**不是追加 —— 新教训要么并进已有条目，要么替换掉过时的。

---

## 五、目录结构

```
nlp-learning/
├── CLAUDE.md            ← 本文件：辅导规则 + 当前快照（每次会话自动加载，必须定长）
├── NOTATION.md          ← ⚠️ 数学符号单一事实来源。讲带符号的内容前必读
├── PROGRESS.md          ← 进度表、错题本、能力画像、会话日志（定长，覆盖式更新）
├── progress/            ← 详细批改记录，按模块拆分。平时不加载，需要回溯时才读
│   ├── L0.md
│   ├── M1.md
│   └── M2.md
├── README.md            ← 总路线图与使用说明
├── SETUP.md             ← 两台机器的环境搭建
├── syllabus/            ← 阶段 1–3 的详细大纲与验收标准
├── assessments/         ← 测评题
├── 00-foundations/      ← 阶段 1：讲义 + 代码练习
├── 01-transformer/      ← 阶段 2
├── 02-llm/              ← 阶段 3
├── 03-research/         ← 阶段 4：论文笔记、复现
├── notes/               ← 他自己的笔记
└── data/                ← 数据集（不入 Git）
```

---

## 六、会话协议（关于档案何时被读取 — 重要）

`CLAUDE.md` 在**会话启动时加载一次**，之后不会自动重读。这带来三条必须遵守的规则：

### 对学习者（完整版见 `README.md`）

1. **换设备后不要恢复旧对话，开新会话。** 旧对话装的是它启动那一刻的档案。
   **档案才是记忆，对话只是当次的工作台。**
2. **开始：** `git pull` → 在项目目录打开 Claude Code → 说「**继续学习**」
3. **结束：** 说「**更新档案**」→ Claude 写完后 `git add -A && git commit && git push`。
   **没 push 就等于没学。**
4. 自己手动改过档案 → 在对话里说一声「档案我改过了」。

### 对 Claude

- 听到「**继续学习**」时，**显式用 Read 工具重读** `CLAUDE.md` 和 `PROGRESS.md`，
  不要依赖会话启动时的自动加载 —— 那份可能已经过期。
- 听到「**更新档案**」时，**按规则 12 分流**：
  详细批改 → `progress/M*.md`（追加）；错题本/能力画像/会话日志 → `PROGRESS.md`（覆盖）；
  当前快照 → `CLAUDE.md` 第三节（覆盖）。写完提醒学习者 commit + push。
- 在同一会话内每次修改档案后，**告诉学习者需要 push**，不要默默改完就算了。
