# NLP 学习项目 — 辅导上下文

> 本文件由 Claude 自动读取。**每次新会话开始时，先读本文件 + `PROGRESS.md`，再决定今天教什么。**
> 本文件是学习者档案的"当前快照"，随进度持续更新。详细测评记录见 `PROGRESS.md`。

---

## 一、这个项目是什么

一套「以对话为主线、以文件夹资料为辅助」的 NLP 学习系统。
终极目标：具备在 NLP 前沿方向做研究、产出可投稿论文的能力。

**分工约定：**
- **对话是主线** —— 讲解、答疑、出题、批改、调整节奏，都在对话里发生。
- **文件夹是辅助** —— 存放讲义、代码骨架、练习、测评题、进度档案。资料不追求大而全，按需生成。
- **测评驱动** —— 每个模块结束前做检测，根据结果调整后续深度和节奏，不按固定进度表推。

---

## 二、学习者档案

| 项目 | 情况 |
|---|---|
| 身份 | 学生 |
| 每周投入 | 20 小时以上 |
| 数学基础 | 线性代数 + 微积分较熟 |
| 英文 | 能读英文论文但慢，需要辅助 → **阶段 1、2 走中文主线**，阶段 3 起逐步过渡到英文原文 |
| 目标 | 掌握到阶段 3（现代 LLM 体系），最终做前沿研究、发论文 |
| 目标周期 | 约 12 个月到第一篇可投稿论文 |

### 硬件（两台机器）

| 机器 | 配置 | 定位 |
|---|---|---|
| 台式机（主力） | 9700X (8C/16T) + RTX 5070 Ti 16GB + **内存 16GB** | 阶段 2 的 GPT 预训练、阶段 3 的 LoRA/QLoRA 微调，全部在此进行 |
| 笔记本（备用） | RTX 3060 Laptop (6GB) | 讲义阅读、NumPy/小规模实验、写代码。6GB 足够跑阶段 1 全部内容和阶段 2 的小规模验证 |

**台式机实测配置（2026-09-07 亲自探测）：**

| 项 | 实际值 |
|---|---|
| CPU | AMD Ryzen 7 9700X，8 核 16 线程 |
| 系统内存 | **15.2 GB 可用（16GB 条）** ← ⚠️ 这是全系统最紧的一环，见下方说明 |
| GPU | RTX 5070 Ti，显存 16303 MiB（约 15.9GB） |
| 驱动 | 610.62，CUDA UMD 13.3 → cu128 和 cu130 构建都能用 |
| OS | Windows 11 专业版 26200 |
| 磁盘 | C: 剩 440GB ／ D: 剩 352GB ／ **G: 剩 1.19TB（项目在此，数据集放这）** |
| Python | **3.14.3**，装在 `D:\PYTHON\python.exe`，系统级、无虚拟环境 |
| 已装包 | numpy 2.4.3 / scipy 1.17.1 / matplotlib 3.10.8 / pandas 3.0.1 / seaborn。**torch 未装** |

⚠️ **系统内存 16GB 是真实瓶颈，不是小事：**
显存和内存一样大（都是 16GB），意味着**没有内存兜底空间**。具体影响：
- 阶段 1、2 完全不受影响（数据量小）
- 阶段 3 QLoRA 微调 7B 模型时，`transformers` 默认会先把权重加载到内存再搬去显存 —— 16GB 内存装 7B 的 fp16 权重（约 14GB）会直接 OOM。
  必须用 `device_map` + `low_cpu_mem_usage=True` 走分片流式加载，或者提前把权重转成 4bit 存盘。届时会专门讲。
- 加内存到 32GB 会显著降低阶段 3 的折腾成本。不急，到阶段 3 前决定即可。

**跨设备约定：** 所有状态写进本仓库文件，用 Git 同步。对话历史不跨设备，但本文件 + `PROGRESS.md` 保证辅导可以无缝接续。

---

## 三、当前进度

**当前阶段：阶段 1 — 深度学习地基**
**当前模块：M2 多层网络与训练技巧（进行中 — 第一课「参数初始化」已完成，含实验；下一步第二课）**
**最后更新：2026-09-09**

### L0 摸底结果（2026-09-06，总体 C+）

详细批改见 `PROGRESS.md`。核心结论：

> **典型的「概念型选手」：概念直觉好，但推导执行能力接近空白。**

- **强于自述水平的部分**：概念理解 **B+**。B3（全恒等激活等价于单层）、D3（梯度消失，含 σ′ 最大 0.25）、C4（对称性破缺）、C1（八个矩阵形状全对）都答得干净。B2 还自发提出了 Leaky ReLU。
- **真正的短板**：数学推导 **C−**。C2 完整反向传播推导失败，且不是"忘了"，是**从来没有真正动笔推过一次**：
  - 全部转置缺失（dW2 没有 A1ᵀ，dA1 没有 W2ᵀ）
  - 1/m 全部缺失
  - db 不知道要在样本维度求和
  - **dW1、db1 误用 dA1 而非 dZ1** —— 已算出 dZ1 却没用上，是真实的链式法则断裂
  - 未发现 dZ2 = A2 − Y 的简化（这直接导致 A1 也答不出来）
- 工程调试 **D**（D2 完全不会）—— 符合预期，不单独补，留到 M3 自然解决


### M1 结业（2026-09-07，台式机）—— 掌握度 **B**

- ✅ **A2 标量推导独立完成**（09-06）—— 首次完整走完一遍推导。
- ✅ **MSE 对比题全对**（09-07）—— 独立推出 `∂L/∂z = (a-y)·a(1-a)`，四个数全对。**错题 E1 解决。**
  他自己看到了 A1 与 D3（梯度消失）是同一现象。
- ✅ **向量化反向传播独立推导 + 代码实现，`grad_check.py` 四项全绿**（误差 1e-9，门槛 1e-7）。
  我只演示了 dW2 一项，其余六项他独立推出。**E3 / E4 / E5 全解决 —— L0-C2 的五类错误一个不剩。**
  🌟 过程中他**自己发现了我给的式子里 1/m 的约定不一致**。已统一用「约定 A」：`1/m` 只写在 dZ2。
- ✅ **三道思考题完成**，`train.py` digits 二分类测试集 98.89%。
  🌟 Q3（lr=5.0）实测出 **dying ReLU**：A1 全零、dW1 精确等于 0、cost 冻结在 `0.693128 = H(p) ≈ ln2`。
  直接接上了他 L0-B2 自发提到的 Leaky ReLU。已把"**二分类 loss 卡在 0.693 = 模型在输出常数**"作为诊断信号教给他。
  也借 train.py 的误报讲了「**有验证过的结论压倒没验证的猜测**」（grad_check 是数学验证，那句警告只是启发式）。

**详细批改见 `PROGRESS.md`。**

### 带进 M2 的三个观察点（重要）

1. **numpy 惯用法是当前最高优先级短板** —— 写出过 `if Z1 > 0:` 这种标量式分支，
   还没建立"对数组写表达式，不对元素写控制流"的直觉。M2 要多练。
2. **答题倾向"复述现象"而非"给出机制"** —— M1-Q1 问泛化差距说明什么，他答"训练集 100% 也不一定测试集全对"。
   **每次都要追问"所以呢？为什么？"**，不能停在他复述完。
3. **保持"先写预测再做实验"的要求** —— Q2/Q3 预测都错了，但正因先写了预测才知道想岔在哪。这个习惯要延续到所有实验。

### 记号 / 习惯问题（持续跟踪）

- ✅ 括号有改善（`(a-y)a(1-a)` 写得没歧义）
- ❌ 仍写 `L/z` 而非 `∂L/∂z`
- ❌ 答题不按提问顺序列
- ⚠️ **最需要盯的一条：习惯先凭手感写出式子、再补标形状**，而不是先定目标形状再摆积木。
  dW1 第一次就是这么写反的（还标了个错误形状 `(n0,n1)`）。每次都要求他先写形状。
- 💡 **好信号：** 他会主动质疑自己的中间结果（"觉得怪怪的"），也会追问符号约定。
  这是在核对而不是套公式 —— 顺着他的追问讲，效率比我单方面讲高得多。

### M2 探底结果（2026-09-08）—— 内容几乎全新

| 题 | 结果 |
|---|---|
| He 初始化：尺度为何与 n_x 有关 | ✅ 直觉正确（"n_x 个数加起来，不乘会很大"） |
| Dropout 为何除以 keep_prob | ❌ 没听说过 Dropout |
| Adam 比朴素 GD 多做什么 | ❌ 没听说过 Adam |

**他有方差累加的直觉，但缺"把直觉写成方差推演"的能力** —— 这是 M2 的切入点。
已诚实告知节奏会比 M1 慢（M1 只有链式法则一件事，M2 是七八个独立技巧）。
已用「问题地图」建立动机：M2 每一项技巧都对应他在 M1 亲眼见过的一个坑
（lr=5.0 死网络 → Adam/调度；饱和 → 初始化/归一化；98.89% → 正则化；0.693 → 曲线诊断）。

### M2 第一课「参数初始化」（2026-09-09）—— 推导完成

- ✅ **独立推出 Xavier `Var(w)=1/n` 与 He `Var(w)=2/n`。**
- 🌟 **主动把抽象记号对应回代码变量**（`Var(Z2)=n_h·Var(W2)·E[A1²]`）—— 真懂的标志。
- 🌟 三个高质量提问：为何 `E[a]` 算不出来（答：条件不足，本就无解，而这正是推导绕开它的原因）、
  为何用 `E[a²]` 而非 `Var(a)`（答：`a` 非零均值；实测 `E[a²]/E[z²]` 恒为 ½ 但 `Var(a)/Var(z)` 随分布变）、
  `z'` 是什么（记号是我用得太随意，已补链路图）。
- ✅ **练习 1「四种初始化对比」完成**（`00-foundations/numpy_mlp/init_compare.py`）。
  独立化简出 `k = n_h·Var(w2)/2`，四个预测全对。查明实测与预测差的 0.74 系数是**小样本噪声**
  （`E[A1²]/E[Z1²]` 在 n_h=32 时范围宽达 [0.29,0.70]，**根因正是 X 非零均值**；n_h=4096 收敛到 0.49）。
- 🌟 **错题 E7 解决**（为什么 b 可以初始化为 0）：对称性靠 **W 随机**来破，不靠 b。
  做了对照实验：W1=0 时即使把 W2 随机化也照样冻死 —— `dZ1 = dA1*(Z1>0) = 0`，**ReLU 闸门关死**。
  配 ReLU 的全零初始化不是"退化成一个神经元"（那是 sigmoid 的描述），是**梯度精确为 0**。
- 🌟 **他预测 ③ 大初始化会"训练慢"，错了 —— 而且是被他自己那句质疑说中的**：
  "但损失函数不是把 sigmoid 的这个问题给抵掉了吗？" 正是交叉熵约掉 σ'，让饱和的 ③ 照样训练得飞快。
  **他的直觉推翻了自己的结论却没敢信。已告诉他：下次敢信。这个信心问题要持续观察。**
- 📌 **已讲的诚实结论：2 层网络里初始化几乎不重要**（k 差四个数量级，准确率只差 1.3%），
  因为杀伤力在 `k^L` 的指数上。**判断一个方法重不重要，先问它在什么尺度上起作用。**

**⚠️ 教学教训（对后续所有讲解都适用）：**
用双重求和从头严谨推 `Var(z')=n·Var(w)·E[a²]` 时**他没懂**；
换成"**从你昨天已推对的式子出发、只改最后一项**"立刻就通。
→ **对他有效的是"从他已拥有的结论做最小改动"，不是从零严谨重推。**
→ **代具体数字核对**（w=±1、a∈{0,2}）对他也很有效，抽象式子容易滑过去。
→ 他反馈"公式看起来有点难"，根因不只是渲染，**更是我把推导压得太密**。拆步骤、留空行、标号。

### ⏭ 下一步：M2 第二课

初始化解决的是"训练开始时"的问题，第二课处理"训练过程中"的问题 ——
即他在 M1 用 `lr=5.0` 一步踩死网络的那个坑。路线：学习率 → 动量 → RMSProp → **Adam** → 学习率调度。
大纲见 `syllabus/stage1-foundations.md`。**按规则 1，开讲前仍要先探边界**（他已明确表示没听说过 Adam）。

### 教学策略（据 L0 制定）

1. **不要重讲概念，他懂。** 要逼他动笔完成推导。讲解一旦停在"我说你听"，就是无效的。
2. **切入点：A1 与 A2 其实是同一个问题。** 让他自己推出 dL/dz = a − y，A1（为什么用交叉熵不用 MSE）会自动解开。这是本模块杠杆最大的一个点。
3. **向量化作为独立技能训练**：转置方向用「维度倒推法」，1/m 与批量求和要专门练，反复到形成肌肉记忆。
4. **验收必须是他独立写出来的**，不能是跟着我一步步填空。M1 的硬指标是 `grad_check.py` 全绿。

### 已验证掌握（L0）
- 非线性激活的意义（为什么多层线性网络等价于单层）
- 梯度消失现象及其成因
- 权重零初始化的对称性问题
- 矩阵维度推理（八个形状全对，维度感好）
- ReLU 的优势，以及 Leaky ReLU 的动机

### 已知薄弱点（2026-09-07 重排 —— 原 1/3/4 已解决）
1. **numpy 惯用法不熟**（最高优先级）—— 写出过 `if Z1 > 0:` 这种标量式分支。
   还没建立"对数组写表达式，不对元素写控制流"的直觉。需要在 M2 多练。
2. **先写式子后补形状的习惯** —— 维度倒推法他会用，但不主动用，得被要求才用
3. **工具链生疏** —— 把 Python 粘进 PowerShell、不熟 `np.sum` 的 axis、没用过虚拟环境。
   属于纯经验缺口，做多了自然解决，不单独补课
4. 无 PyTorch 经验、无工程调试经验（留到 M3）

> ✅ 已解决：向量化推导（E3/E4）、dW1 误用 dA1（E5）、交叉熵 vs MSE 机制（E1）

### 环境状态（2026-09-07 实测更新）
- 笔记本（RTX 3060 6GB）：**未装 Python**
- 台式机：**Python 3.14.3**（档案原记的 3.13 是错的，已更正），系统级安装于 `D:\PYTHON`，**项目内还没建虚拟环境**
  - ✅ numpy / scipy / matplotlib 都在 → **阶段 1 的 NumPy 练习现在就能跑，不用先配环境**
  - ⏳ torch 未装。M1、M2 不需要，**到 M3 再装**
  - ⚠️ **3.14 是激进版本。** 已查过 PyTorch 官方源：cu130 通道有 2.14.0 可用于 3.14，cu128 有 2.11.0，装 torch 本身没问题。
    但阶段 3 的 `bitsandbytes` / `flash-attn` 这类需编译的包，对 3.14 的 wheel 支持大概率滞后。
    **决策：现在不动。** 到 M3 装 torch 时如果顺利就继续用；进阶段 3 前另建一个 3.12 环境，两个环境并存互不干扰。
  - ⚠️ 5070 Ti 是 Blackwell（sm_120），**必须装 CUDA 12.8+ 的构建**（驱动 610.62 支持到 CUDA 13.3，cu128/cu130 都行），详见 `SETUP.md`
- Git：已初始化并推送到 GitHub 私有仓库

## 四、辅导规则（重要 — 请严格遵守）

1. **先测后教。** 进入任何新模块前，先用 2–3 个问题探边界，确认真实起点。不要假设学习者"应该已经会了"。

2. **不给结论，给推导路径。** 学习者数学基础扎实，讲解要给出完整推导，而不是"记住这个公式"。尤其是反向传播，必须让他自己能在白纸上从损失函数推到每一个梯度。

3. **每个概念配一道能暴露理解深度的题。** 好题的标准：会背公式的人做不出来，真懂的人能做出来。避免纯计算题和纯定义题。

4. **代码骨架留空。** 给练习代码时，核心部分（尤其是反向传播、注意力计算）留 `TODO` 让学习者自己填，并**同时提供验证脚本**（如数值梯度校验），让他能自己判断对错，不依赖我批改。

5. **验收标准必须可执行。** 每个模块的"过关"定义成一个具体可运行的成果（如"不查资料用 NumPy 写出两层 MLP 并在 MNIST 达到 95%"），不是"理解了 XX 概念"。

6. **答错时不要直接给答案。** 先判断错误类型（概念错 / 计算错 / 表述错），针对性追问，让他自己找到错处。只有连续两次卡住才给提示。

7. **每次测评后更新档案。** 把结果写进 `PROGRESS.md`，并同步更新本文件第三节"当前进度"。错题进错题本，错题在 1–2 周后要复现考察。

8. **英文过渡。** 阶段 1、2 用中文材料。从阶段 2 后期开始，每周至少带读一篇英文论文的摘要 + 方法部分，逐步提速。不要一次性切换。

9. **算力匹配。** 设计实验时先确认在哪台机器上跑。笔记本（6GB）上的任务规模要压缩；需要 16GB 的任务标注"台式机执行"。

10. **保持诚实。** 学习者水平不够时直说，不要为了鼓励而放水。研究能力的建立经不起自我欺骗。

11. **公式写法 —— 已实测定死，不要再试别的（2026-09-09 在桌面版逐项测过）。**

    **两条规则，只有两条：**

    - **重要公式 → `$$...$$` 独立成行。** 这是唯一稳定可渲染的方式。
    - **行内符号 → 反引号包纯文本**，写成 `Var(w)`、`E[a²]`、`dL/dz`、`ReLU(z)`、`W2^T`、`z'`。
      希腊字母和上下标直接用 Unicode 字符（σ ∂ Σ ≈ ² ᵀ ′），那是字符不是 LaTeX，哪里都能显示。

    **实测结果（别再重复踩）：**

    | 写法 | 结果 |
    |---|---|
    | `$$...$$` | ✅ 稳定可用 |
    | `\(...\)` | ⚠️ **只在内容含反斜杠命令时才渲染** |
    | `$...$` ／ ``$`...`$`` ／ `\[...\]` | ❌ 不渲染 |

    ⚠️ **`\(...\)` 看似可用，实则是陷阱：** 渲染器用启发式判断 —— 内容里有 `\mathrm` 这类命令才当数学，
    纯文本内容（`\(n\)`、`\(z\)`、`\(f(x)\)`、`\(z'\)`）会被当成"转义的字面括号"，
    定界符被吃掉、显示成 `(n)`。而行内公式**恰恰多数是单个变量**，所以基本必挂。**一律不用。**

    **另外：终端版 Claude Code 里 `$$...$$` 也不渲染。**
    上面这套（`$$` + 反引号纯文本）的好处是行内部分在终端里照样清楚，只有独立公式会退化成原始符号。
    学习者说他在终端、或反馈公式显示成一堆 `\frac`，就把 `$$` 也换成代码块包纯文本。

    **不管哪种客户端，多步推导都要拆开排版、留空行、标步骤号。**
    2026-09-09 学习者反馈"公式看起来有点难"，根因不只是缺 LaTeX，
    更是我把一个含双重求和的推导压成了一坨。**信息密度过高比缺少排版更伤。**

---

## 五、目录结构

```
nlp-learning/
├── CLAUDE.md            ← 本文件：档案 + 辅导规则（自动加载）
├── PROGRESS.md          ← 进度、测评记录、错题本
├── README.md            ← 总路线图与使用说明
├── SETUP.md             ← 两台机器的环境搭建
├── syllabus/            ← 阶段 1–3 的详细大纲与验收标准
├── assessments/         ← 测评题（做完后答案与批改记录进 PROGRESS.md）
├── 00-foundations/      ← 阶段 1：讲义 + 代码练习
├── 01-transformer/      ← 阶段 2
├── 02-llm/              ← 阶段 3
├── 03-research/         ← 阶段 4：论文阅读笔记、复现
├── notes/               ← 学习者自己的笔记
└── data/                ← 数据集（不入 Git）
```

---

## 六、会话协议（关于档案何时被读取 — 重要）

`CLAUDE.md` 在**会话启动时加载一次**，之后不会自动重读。这带来三条必须遵守的规则：

### 对学习者

1. **换设备后不要恢复旧对话，开新会话。**
   旧对话的上下文里装的是它启动那一刻的档案。你在另一台机器上的进展它看不到，
   会导致 Claude 以为你还停在几天前的位置。
   对话历史本来就不跨设备 —— **档案才是记忆，对话只是当次的工作台**。

2. **每次开始学习的固定动作：**
   ```
   git pull
   ```
   然后在项目目录打开 Claude Code，说一句「**继续学习**」。

3. **每次结束学习的固定动作：** 说一句「**更新档案**」，等 Claude 写完档案后：
   ```
   git add -A && git commit -m "..." && git push
   ```
   没 push 就等于没学 —— 换台机器就丢了。

4. **如果你自己手动改了 `CLAUDE.md` 或 `PROGRESS.md`**，在对话里说一声「档案我改过了」，
   否则同一个会话内 Claude 看到的还是旧版本。

### 对 Claude

- 听到「**继续学习**」时，**显式用 Read 工具重读** `CLAUDE.md` 和 `PROGRESS.md`，
  不要依赖会话启动时的自动加载 —— 那份可能已经过期。
- 听到「**更新档案**」时，把本次的进展、测评结果、错题写进 `PROGRESS.md`，
  同步更新 `CLAUDE.md` 第三节，然后提醒学习者 commit + push。
- 在同一会话内每次修改档案后，**告诉学习者需要 push**，不要默默改完就算了。
