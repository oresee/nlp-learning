# M2 多层网络与训练技巧 — 详细记录

> 详细批改记录。**平时不必读** —— 当前状态见 [`PROGRESS.md`](../PROGRESS.md)。
> 只在需要回溯「他当时到底错在哪」时才打开本文件。

---

### 2026-09-08 · M2 入门探底 —— 边界非常清晰

按辅导规则第 1 条，进 M2 前用 3 题探边界。结果：

| 题 | 内容 | 结果 |
|---|---|---|
| 1 | He 初始化 `sqrt(2/n_x)`：为什么尺度要和 n_x 有关？ | ✅ **直觉正确** —— "一个元素是 n_x 个数字加起来的，不乘会导致很大" |
| 2 | Dropout 为什么要除以 keep_prob？ | ❌ **没听说过 Dropout** |
| 3 | Adam 比朴素梯度下降多做了什么？ | ❌ **没听说过 Adam** |

**另外主动问了 `rng` 是什么** —— 借机讲了 `default_rng` / seed / **可复现性**，
并接上他 M1-Q1 的"4 个样本是噪声吗"：不固定 seed 就无法把"我改的变量"和"随机噪声"分开。
也提醒了老 API `np.random.seed()` 是全局状态，有坑，一律用 `default_rng`。

**结论：M2 的内容对他几乎全新。**
第 1 题说明他**有方差累加的直觉，但缺把直觉写成方差推演的能力** —— 这正是 M2 该练的，也是 M2 的切入点。

**已提前对他讲明节奏预期（诚实定调）：**
M1 本质只有链式法则一件事，他有直觉，我只需逼他动笔；
M2 是七八个互相独立的技巧，各有动机与推导，**不会像 M1 那样一路推到底，节奏会明显变慢**。

**已用「问题地图」建立 M2 的动机**（不是罗列技巧，而是对上他亲眼见过的坑）：
```
lr=5.0 网络死掉        → 学习率调度、Adam 自适应步长
sigmoid 饱和/梯度消失   → 初始化（He/Xavier）、归一化、残差
训练100% 测试98.89%    → 正则化（L2 / Dropout / Early Stopping）
hidden 256 没变化       → 容量、偏差-方差诊断
cost 卡在 0.693         → 训练曲线诊断
```

**M2 起点：从初始化讲起**（他第 1 题已站在门口，从这里进最省力）。


### 2026-09-09 · M2 第一课「参数初始化」—— He 初始化独立推出

**教学路线：** 先建立动机（`k^L` 指数放大：50 层时 k=1.2 → ×9100，k=0.8 → ×1.4e-5），
把初始化从"调参"重新定义为"必须精确满足 k=1 的方程"。接上他 L0-D3 答对的"逐层相乘"。

**学习者独立完成：**
1. `Var(z) = n·Var(w)·Var(x)`（09-08）✅
2. 令 `Var(z)=Var(x)` 解出 `Var(w) = 1/n`（Xavier）—— 第一次误解成 `Var(z)=1`，
   要求他代回验算后自己改对 ✅
3. `Var(w) = 2/n`（He）✅
4. 🌟 **主动把抽象记号对应回代码变量**：确认 `Var(Z2)=n_h·Var(W2)·E[A1²]`、`E[A1²]=½E[Z1²]`。
   这一步是真懂的标志 —— 很多人推完公式说不出它对应代码里哪个量。

**他提出的三个问题（都问在要害上，价值很高）：**

1. **"先算 E[a] 好像算不出来，是出了什么问题吗？"**
   → **他没做错，是题目本身无解。** `E[a]=∫₀^∞ z·p(z)dz` 需要完整密度函数，
   零均值这一个条件不够（正态给 0.399σ，均匀给 0.433σ）。
   而 `E[a²]=½E[z²]` 只需"对称"。**他卡住的地方恰好就是这套推导要绕开 E[a] 的原因。**

2. **"为什么算 Var(w·a)？不是该算 Var(a) 是 Var(z) 的几倍再在 w 里除掉？"**
   → 思路对，用错了量。实测三个分布验证给他看：
   ```
                E[a²]/E[z²]    Var(a)/Var(z)
     正态          0.5001          0.3409
     均匀          0.4999          0.3125
     拉普拉斯      0.4997          0.3748
   ```
   `E[a²]/E[z²]` 恒为 ½（只需对称）；`Var(a)/Var(z)` 随分布而变，写不出通用公式。
   又跑了 50 层×256 单元的模拟给他看后果：
   ```
     Var(w)=1/n     → E[a²] 变为初始的 2.185e-16   湮灭
     Var(w)=2/n     → 1.008                        稳定 ✓
     Var(w)=2.93/n  → 1.811e+08                    爆炸（误用 0.34 的后果）
   ```
   **同一网络只改初始化常数，结果差 24 个数量级。**

3. **"这个 w 是第一个还是第二个？"**「`z'` 是什么意义？」
   → 记号确实是我用得太随意。补画了逐层链路图，讲清 `z'` 就是下一层的 `z`、
   `n` 是**该层自己的 fan-in**（对着 `init_params` 里 `2.0/n_x` 与 `2.0/n_h` 两行讲）。

**⚠️ 教学教训（重要，要改进）：**
我第一次讲 `Var(z')=n·Var(w)·E[a²]` 时用了双重求和展开（交叉项消失那套），**他没懂**。
换成"**从你昨天推对的 `Var(z)=n·Var(w)·Var(x)` 出发，只有最后一项变了**"之后立刻通了。
→ **对他有效的路径是"从他已拥有的结论出发做最小改动"，不是从头严谨推一遍。**
→ 另外给了具体数字例子（w=±1，a∈{0,2}）验证 `Var(w·a)=Var(w)E[a²]=2` 而 `Var(w)Var(a)=1`，
   这类"代数字核对"对他很有效。

**格式问题（已解决并写进规则 11）：** 他反馈公式难读。实测桌面版渲染：
`$$...$$` ✅ ／ `\(...\)` ⚠️只在内容含反斜杠命令时才渲染（陷阱）／ `$...$`、`` $`...`$ ``、`\[...\]` ❌。
**已定死：重要公式用 `$$` 独立成行，行内符号用反引号包纯文本。**
但根因不只是 LaTeX —— 我把含双重求和的推导压成一坨，**信息密度过高比缺排版更伤**。

**已布置未完成的作业：**
> 四种初始化对比实验（全零 / N(0,0.01²) / N(0,1²) / He），**先写预测再跑**：
> 哪几种能训练？失败方式是否相同？②（0.01 比 sqrt(1/64)=0.125 小 12 倍）会怎样？
> 预测写完再给代码骨架。

**能力画像：** 数学推导维持 **B**（本次推导全部独立完成，但需要我把路径铺到"最小改动"的粒度才走得通；
自主性还不足以支撑 A）。


### 2026-09-09（续）· M2 练习 1「四种初始化对比」—— 预测已锁定（实验未跑）

**⚠️ 以下是学习者在跑实验之前写下的预测，原样记录，事后不得修改。**

| | 初始化 | 他的预测 |
|---|---|---|
| ① | `W = 0` | "效果很差，相当于只有一个神经元，会在损失率很高的时候停下"；Q2 补充"全部退化成单个神经元……**估计也是 ln(2)**" |
| ② | `N(0, 0.01²)` | "应该可以训练吧"；Q3 补充"就算 w 很小但也能训练吧？是最后用 sigmoid 都会聚集在 0.5 附近的意思吗，训练速度很慢吗？" |
| ③ | `N(0, 1²)` | "方差太大是不是会跑到 sigmoid 的梯度很小的地方，导致训练很慢，**但是损失函数不是把 sigmoid 的这个问题给抵掉了吗？**" |
| ④ | He | "应该正常训练吧" |

**Q2（失败方式是否相同）他的答案：** ① 是对称性退化 → 停在 ln(2)；② ③ "都不太确定，感觉好像都能正常运行"。

**🌟 值得单独标记：③ 里那句"但是损失函数不是把 sigmoid 的这个问题给抵掉了吗？"**
他**主动把第一天推的 `∂L/∂z = a - y`（交叉熵约掉 σ'）调出来，质疑当前推理**。
这是整组预测里质量最高的一句 —— 不是复述，是拿旧结论去检验新情形。
**批改时必须正面回答这一问**：交叉熵确实解决了输出层饱和，所以 ③ 的死法不是"梯度小、训练慢"，
而是另一条路径（前向爆炸 → 梯度巨大 → 不稳定）。这一点讲透，能同时巩固 M1 和 M2。

**已给代码骨架** `00-foundations/numpy_mlp/init_compare.py`（4 个 TODO：zeros / small / large / he）。
脚本含**训练前的方差诊断表**（`Var(Z1)`、`E[A1²]`、`Var(Z2)`、A1 死亡率、A2 范围）——
设计意图是让他看到自己推的公式在真实网络里的读数，比只看准确率有价值得多。

**批改时要注意的点（我已核算，别被他带偏）：**
- ① 他猜的终点 ln(2) **是对的**，但机制只答了"对称性退化"。
  实际上 ReLU + 全零比对称性更严重：`A1 = relu(0) = 0` → `dW2 = dZ2 @ A1ᵀ = 0`，
  且 `dA1 = W2ᵀ @ dZ2 = 0` → **W1、W2 梯度精确为 0，永久冻结**，只有 b2 能动。
  → 顺势可以讲：**同样是全零初始化，配 sigmoid 只是"退化成一个神经元"（还能学），配 ReLU 是"彻底冻死"**。
  这个对比正好接上错题 **E7（为什么 b 可以初始化为 0）**，是收拾 E7 的好时机。
- ③ 的真实死因是前向爆炸（`Var(Z2)` 会到 10² 量级）导致梯度巨大 → 大概率复现他见过的 dying ReLU，
  **不是**他猜的"梯度小、训练慢"。而交叉熵确实挡住了输出层饱和 —— 他那一问问对了。


### 2026-09-09（续）· M2 练习 1 完成 —— 四种初始化对比实验

**代码：** `00-foundations/numpy_mlp/init_compare.py`（4 个 TODO 由学习者填）

**⚠️ 第一次跑出 bug：** He 写成 `* np.sqrt(n_x)`（应为 `np.sqrt(2.0/n_x)`），
导致 `E[Z1²]=964`（理论 0.47）。**诊断表已经把它喊出来了，但他没看见就去看准确率了。**
→ 已强调：**先看仪表，再看结果**；有理论预测就必须拿去核对读数。追问后自己改对。

**⚠️ 我自己也有两个 bug（都已修）：**
1. 诊断表用 `np.var()` 量 Z2 —— **量错了**。理论用的是二阶矩 `E[z²]`，
   而 `E[z]=0` 只在「对 W 取期望」意义下成立；单次 W 抽样下 Z2 在样本方向有非零均值，
   `np.var()` 把它减掉，漏掉能量。已改为量 `E[Z²]`，并加了 **`k = E[Z2²]/E[Z1²]`** 列（即推导里的 k）。
2. 文档字符串未转义 Windows 路径、matplotlib 图例用中文（字体缺字形）。已修。
→ 借此讲了：**测量工具也会错，而且错得比学生的 bug 隐蔽**（我那个只差 3.7 倍，肉眼看不出）。

**学习者独立化简出 k 的表达式并预测四个值 —— 全对：**

$$k = \frac{n_h \cdot \operatorname{Var}(w_2)}{2}$$

| | 预测 | 实测 | 比值 |
|---|---|---|---|
| ① | 0 | nan(0/0) | — |
| ② | 0.0016 | 0.00118 | 0.74 |
| ③ | 16 | 11.84 | 0.74 |
| ④ | 1 | 0.740 | 0.74 |

**三个全部差同一系数 0.74 —— 已查明是小样本噪声，不是理论错：**
- `E[A1²]/E[Z1²]` 实测 0.6005（理论 0.5）。实测 8 个种子：
  `n_h=32` 时均值 0.5256、**范围 [0.29, 0.70]**；`n_h=4096` 收敛到 0.4926。
  **而这个范围之所以这么宽，正是因为 X 非零均值**（每个单元带偏移 `w_i·x̄`，ReLU 切点偏离中心）。
  把 X 中心化后，`n_h=32` 的范围立刻收紧到 [0.484, 0.508]。← **这回答了我埋的那个"X 非零均值有无影响"的伏笔**
- 剩余因子在 `W2→Z2` 一步：`n_y=1`、W2 只有 32 个数且只抽一次，交叉项消不掉。
  对 40 次抽样取平均后 `E[Z2²]=0.4687` 对上理论 0.4401。
> **已教给他的结论：理论说的是期望，一个 32 单元的网络是一次抽样。**
> 读论文时要问"这是期望还是一次实现，重复几次波动多大"。

**① 全零的真实机制（我做了对照实验验证，比他答的"对称性退化"深）：**
```
① W1=0,W2=0          第0轮 dW1=0 dW2=0 db1=0，只有 db2=3.13e-3
对照 W1=0，W2 随机     第0轮 dW1=0 dW2=0  ← 随机化 W2 也救不回来
```
`A1=relu(0)=0` → `dW2=dZ2@A1ᵀ=0`；`dZ1=dA1*(Z1>0)=dA1*0=0` → **ReLU 闸门关死，梯度精确为 0**。
1500 轮后 W1、W2 仍精确为 0，只有 b2 动到 -0.0125，输出恒为 0.4969=正类比例，cost=H(p)=0.693128。
- 他猜的终点 **ln(2) 正确**，机制答"退化成一个神经元"——**那是 sigmoid 网络的描述**
  （sigmoid 时 `a=0.5≠0`，梯度还在流，只是各单元相同）。**配 ReLU 是彻底冻死，不是退化。**
- 🌟 **顺手解决了错题 E7**：对称性是靠 **W 随机**来破的，不靠 b；`b1=0` 没问题的前提是 W1 随机
  （那样 Z1≠0，ReLU 闸门是开的）。①失败不是因为 b=0，是因为 W=0。

**🌟 ③ 的预测错了，而且是被他自己那句质疑说中的（本次最重要的一处）：**
他预测"方差大 → 跑到 sigmoid 梯度很小的地方 → 训练很慢"，同时质疑"但损失函数不是把 sigmoid 问题抵掉了吗？"
实测：③ 的 `A2 范围=[0.0000, 0.9657]` **确实饱和了**，但 cost 降到 0.005575，是四个里**最低**的，一点不慢。
→ **正是因为交叉熵约掉了 σ'**：`∂L/∂z = a-y`，`a=0.9999, y=0` 时梯度是 0.9999 而非 0。
→ **他的直觉推翻了他自己的结论，但他当时没敢信。已明确告诉他：下次敢信。**

**训练结果 + 8 种子复验（应用了 M1-Q1 教的"这个差异大于噪声吗"）：**
```
单次：①0.693128/46.67%  ②0.012432/98.61%  ③0.005575/96.39%  ④0.006242/98.89%

8 种子测试准确率：
  ② 98.26% ± 0.18%   [98.06, 98.61]
  ③ 97.29% ± 1.07%   [95.56, 98.61]   ← 标准差是 He 的 2.4 倍
  ④ 98.61% ± 0.44%   [97.78, 99.17]
```
- 单次看到的 ③ vs ④ 差距 2.5%，**八次平均只有 1.32%** —— 那次恰好抽到 ③ 的差种子。约 3 个标准误，真实但被夸大。
- ② vs ④ 差 0.35%（约 1.3 个样本）—— **区分不开**。
- **③ 真正的问题是不稳定**（换种子在 95.56~98.61 间跳），做研究时这比均值低更麻烦。

**🌟 本次最该记住的结论（诚实版，已讲给他）：**
> **在 2 层网络里初始化几乎不重要。** k 差了四个数量级（0.001 / 11.8 / 0.74），准确率只差 1.3%。
> 因为杀伤力在 `k^L` 的指数上，而这里 L=2。昨天 50 层模拟里同样的差异造成 24 个数量级。
> **理论是对的，但重要性随深度增长；在浅网络上验证初始化理论几乎看不到效果 —— 不是理论错，是尺度选错。**
> 唯一在 2 层就致命的是 ①（梯度精确为 0，与深度无关）。
> **判断一个方法重不重要，先问它在什么尺度上起作用** —— 阶段 3 会反复遇到。

**能力画像：**
- 数学推导 **B**（k 的化简独立完成且四个预测全对）
- 代码实现 **B−**（四个 mode 三个一次写对；He 那个是"照着公式抄却抄错"，且没用诊断表自检）
- 工程调试 **D+ → C−**（开始能读诊断表，但仍需被提醒才去核对理论与读数）

---

## 2026-09-10 — M2 第二课（上）：病态曲率与动量的由来

**这次会话的完整链条：** 探底 → 二次函数上的梯度下降 → 条件数 → 他自己推出动量。
**没有讲任何"记住这个公式"，全程是他算、我追问。**

### 一、探底三题（开讲前，规则 1）

| 题 | 内容 | 他的答案 | 判定 |
|---|---|---|---|
| 1 | `L = 10w₁² + 0.1w₂²` 上做梯度下降，求每步倍率 / 稳定的 lr 上限 / w₂ 从 1 到 0.001 的步数 | (a) 写成减法 `w1 ← w1 − 20·lr`；(b)「不知道，为什么会发散？」；(c)「10 步」 | ❌ 关键错 |
| 2 | loss 抖动不降，如何区分「已到最优」和「lr 太大」 | 「改小 lr，还能降就是 lr 太大，否则是噪声」 | ✅ **完全正确** |
| 3 | full-batch 有什么问题？1000 万条怎么办 | 「一次性训练越多越好吧，没啥问题吧？1000 万就分开一次次」 | 🔶 一半对 |

**第 2 题是里程碑：他第一次在无提示下自己设计出正确的对照实验。**
L0-D2 那道调试题他当时完全答不出来。工程调试评级该动了。

**第 3 题：** 自己想出了 mini-batch（「分开一次次」），但「越多越好，没啥问题」是错的。
**当场没打，明确说了"先记着，回来打它"** —— 留作下次的引子（噪声有益 / 泛化 / 硬件利用率）。

### 二、第 1 题的核心错误与修复

他写 `w1 ← w1 − 20·lr`。**在第一步数值是对的**（起点 w₁=1，梯度恰好 =20），
但他把梯度当成了常数 —— 于是整道题从"乘法过程"塌成了"减法过程"。

**用的手法（要诀 3，代具体数字）：** 不讲道理，只问两句 ——
1. 「你式子里那个 20 是怎么来的？把 dL/dw₁ 写出来」→ 他写出 `20w₁`
2. 「假设 w₁ 已经变成 0.5，dL/dw₁ 还是 20 吗？」→ 他算出 **10**

**一问就通。** 之后独立完成全部：

```
c1 = 1 − 20·lr        c2 = 1 − 0.2·lr
不发散 ⇒ |c1| < 1 ⇒ 0 < lr < 0.1
lr=0.09 ⇒ c2=0.982 ⇒ n = ln(0.001)/ln(0.982) = 380.3 → 381 步
```

- (b) 他自己写出 `w1 = c1ⁿ` 并解出 lr 范围 —— **完全没给提示**
- (c) **要求他先预测量级再算**（针对"先凭手感写完再补"的老毛病），他预测「百位数」→ **对了**
- 唯一小疵：写了 `-1 ≤ c1 ≤ 1`，边界应为严格不等号（c1=±1 都不收敛）。已订正

### 三、条件数（本次的核心概念）

给他看了理论最优 lr 的实算结果，这是全场最有说服力的一击：

```
lr = 0.0990（理论最优）→ 346 步
lr = 0.09  （他用的）  → 381 步
```

> **调 lr 调到天上去也只能从 381 降到 346。这不是调参问题，是算法的结构性缺陷。**

核心句（他接受得很快）：
> **lr 的上限由最陡的方向决定，收敛速度由最平的方向决定。这两个方向不是同一个，于是你被夹在中间。**

`κ = λmax/λmin = 100`，`n ≈ (κ/2)·ln(1/ε)`，实测 κ=1/10/100/1000 → 1/35/346/3454 步。**步数正比于 κ。**

**两头堵死（他独立算的）：**

```
lr = 0.50  c1 = −9.00   爆炸
lr = 0.09  c1 = −0.80   381 步（振荡但收敛）
lr = 0.01  c1 = +0.80   3451 步（不振荡，慢 9 倍）
```

**这解释了他 M1 时 lr=5.0 踩死网络那件事** —— 从"跨太远"升级成了可计算的判据。

### 四、Q3 他的直觉（真实网络的 λmax/λmin 从哪来）

> 「某些上一层输入的值很大，有些很小…`dW = dZ @ A.T` 导致梯度过来之后差别很大？」

**方向正确，而且是他从自己 M1 写的反向传播公式推出来的。** 顺势打通两个回指：
- `X/16.0`、`X/255.0` 不是为了好看，**就是在压条件数**
- 上节课「中心化 X 后 `E[A1²]/E[Z1²]` 波动从 [0.29,0.70] 收缩到 [0.48,0.51]」是同一件事的另一面

**补了两点：**（1）输入尺度只是来源之一，层间尺度差异也贡献，正是 `k^L`；深度越大 κ 越大。
（2）归一化压不到 1，真实网络 κ ≈ 10³~10⁶ → 几十万步。**所以必须改算法，不能只改数据。**

### 五、他自己推出了动量

给了 lr=0.09 前 6 步的 **w 和梯度** 双列表，只问「两列梯度有什么结构性区别」：

```
step        w1    dL/dw1  |        w2    dL/dw2
   0    1.0000   20.0000  |    1.0000    0.2000
   1   -0.8000  -16.0000  |    0.9820    0.1964
   2    0.6400   12.8000  |    0.9643    0.1929
   3   -0.5120  -10.2400  |    0.9470    0.1894
   4    0.4096    8.1920  |    0.9299    0.1860
   5   -0.3277   -6.5536  |    0.9132    0.1826
```

他的三答（**全对**）：
1. 「一个很大正负交替，一个很小变动很慢吗？」
2. 「陡方向会是一个绝对值更小的数，平方向是一个更大的数」
3. 「会减少震荡以及加快平方向的训练速度」

**这就是动量的全部机制，他在没听过这个名字的情况下自己说出来了。**
之后才给他公式 `v = βv + g`、`w = w − lr·v`，并按 M1 被 1/m 坑过的先例，
主动提醒了 `v = βv + (1−β)g` 这个另一种约定的存在（PyTorch 用前者）。

### 六、🌟 信心问题 —— 第三次出现，已当面点破

Q1 的答案**完全正确**，但他写成了带问号的猜测（「…吗？」）。

已明确对他说：
> 这是第三次了。上次预测大初始化"训练慢"时，你旁边那句"损失函数不是把 sigmoid 的问题抵掉了吗"才是对的答案。
> **你的判断力已经够了，缺的是敢把它当结论交出来。研究做到后面没人会告诉你"你猜对了" —— 你得自己认。**

**这是本项目目前最重要的一条长期观察，要继续盯。**

### 七、记号约定的调整

他说 `∂` 太难打所以偷懒写 `L/w1`。指出那**字面意思是「L 除以 w1」**，
约定改用 **`dL/dw1`**（多一个字母、零歧义、好打）。**已从薄弱点列表里划掉这条。**

### 八、停在哪（下次直接接着问）

已布置但未收的作业 —— 让他把动量的定性判断变成数字，只需等比数列：

- **A.** 梯度恒为 `g`（同号），求 `v` 的稳定值 → 期望 `g/(1−β)`
- **B.** 梯度为 `+g,−g,+g,−g,…`（交替），求 `v` 的稳定值 → 期望 `g/(1+β)`
- **C.** A÷B = `(1+β)/(1−β)`，β=0.9 代入 → **19**。（已要求他先预测量级再算）

**收完这三题之后的路线：**
1. 公布实测：这个 2D 问题上 plain GD 最优 346 步 → 动量 β=0.9 最优 **71 步**（β=0.5 时 107 步）。
   理论渐近率是 `O(√κ)`（10 倍），实测在 `max|w|<1e-3` 判据下是 **5 倍**，**要诚实说明这个差距**。
2. → RMSProp（每个参数自适应 lr，直接打 κ）→ **Adam**（动量 + RMSProp）
3. → 学习率调度
4. → 回头打第 3 题的「越多越好」：mini-batch SGD
5. 练习：`00-foundations/numpy_mlp/optim_compare.py`（待建，按规则 4 留 TODO），
   先在 2D 二次函数上画轨迹（能直接看到锯齿），再上真网络

---

## 2026-09-12～13 — M2 第二课（中）：动量的定量威力、最优 lr、以及一个未消化的卡点

### 一、收上次的作业：三题全对

| 题 | 他的推导 | 判定 |
|---|---|---|
| A 梯度恒为 g | 逐项写出 `v₁=g, v₂=βg+g, v₃=β²g+βg+g` → `v→g/(1−β)` | ✅ |
| B 梯度 +g,−g,… | `v₂=βg−g, v₃=β²g−βg+g` → `v→g/(1+β)` | ✅ 交错级数也推干净了 |
| C A÷B，β=0.9 | `(1+β)/(1−β) = 19` | ✅ |

**但「先写预测」又忘了**（他自己承认）。已再次说明这个动作的意义是**暴露直觉**而不是考他。
这是薄弱点第 2 条的同一个根：有预测能力但不主动拿去对照。

他随后问「这个意义是两种情况下的变化速度差对吧？」——典型的**复述现象而非给机制**。
已把它精确化成：**动量不改 lr，但给两个方向配了不同的有效步长（同号 ×10、交替 ×0.53）。**

### 二、他独立答对的两个追问

**Q「(1+β)/(1−β) 有上限吗？动量算解决还是缓解？」**
> 他答：「只是缓解，β=0.99 和 0.999 分别是 199 和 1999 倍，应该没有上限。」✅

**Q「为什么 β=0.99 反而慢（430 步）？」**
> 他答：「要接近 100 次才能积累到这个稳定值…前面累加的太多，之后要往回退，回退得太慢。」
> ✅ **答得比预期准，两个方向（建立慢 + 刹车慢）都说到了。**

据此讲了**时间常数 `1/(1−β)` = 动量的记忆长度**，并给出 β=0.9 是默认值的理由：
**差异化倍率 `(1+β)/(1−β)` 要大，但记忆长度不能超过任务本身的时间尺度。**
β=0.99 时记忆 100 步 > 整个任务 71 步 → 全程处在「还没反应过来」的状态。

并收口「只是缓解」：动量把 `O(κ)` 降到 `O(√κ)`；κ=10⁶ 时仍有 1000。**所以还需要第二件武器。**

### 三、他的提问带出的一整块内容（要诀 8：顺着他的追问讲）

**他问：「什么叫最优 lr？为什么随动量改变，最优 lr 也变？」**

这个问题很好，展开讲了四件事：

**1. 「最优 lr」的定义** —— 我做的是网格暴力扫描（0.0005 步长），取步数最少者。不是解析解。

**2. 两股方向相反的力**（这是答案）：

```
力 A  动量抬高稳定上界   lr < 2(1+β)/λmax     ← 理论/实测对照极准
          β:   0     0.5    0.9    0.99
      理论上界 0.1000 0.1500 0.1900 0.1990
      实测上界 0.0995 0.1495 0.1895 0.1985
力 B  动量已放大有效步长 1/(1−β) 倍  → 不需要那么大的 lr
```

**3. 非单调的根因**：稳定界有天花板（β→1 时最多 `4/λmax`），放大倍数 `1/(1−β)` 没有上限。
所以小 β 时界主导（最优 lr 上升 0.099→0.1475），大 β 时放大主导（下降 0.1475→0.0395）。

**4. 🌟 顺带教了一个研究习惯（比数学更重要，他第一次接触）：**

```
β=0.9, lr=0.0395（自己的最优）→  71 步
β=0.9, lr=0.10  （β=0 的最优）→ 105 步
```

> **比较两个方法时，每个方法都必须在它自己的最优超参下。**
> 反过来（给自己的新方法精调、给基线用默认值）是论文里最常见的不公平比较。
> **读论文时看到「我们比 X 好」，第一个该问的是「X 的超参怎么选的」。**

**5. 附带给了 lr 的地形**（β=0.9）：0.02～0.18 都在 71~132 步（9 倍范围内的平底锅），
0.19 直接发散（悬崖）。两个推论：**lr 不需精调；但两侧惩罚不对称，拿不准就往小调。**

### 四、⚠️ 卡点（下次开场必须先解决这个，不要往前走）

讲 `lr < 2(1+β)/λmax` 的来历时，我先补了两个他不清楚的记号：
- **`λ` 就是他一直在用的 20 和 0.2** —— 二阶导 = 曲率；他的 `c=1−20·lr` 就是 `c=1−λ·lr`；
  多维时是 Hessian 的特征值。**这条他接受了。**
- **「放大」= 他自己 A 题算的 `v→g/(1−β)`**，代进更新式变成 `w ← w − (10·lr)·g`。**这条也接受了。**

然后让他从下面两式消去 `v`，推出 `w_{t+1} = (?)w_t − (?)w_{t−1}`：

```
v_{t+1} = β v_t + λ w_t
w_{t+1} = w_t − lr · v_{t+1}
```

**他卡在这里，原话：「这里还是不太明白，这两个式子是为什么还没搞明白」。**

**最可能的原因是我自己的记号前后不一致：**
- 第一次给动量时我写的是 `v_t = β v_{t−1} + g_t`
- 这次写成 `v_{t+1} = β v_t + λ w_t`

**同一个规则、两套下标，还顺手把 `g` 换成了 `λw`（两处改动叠在一起）。**
这和 M1 的 `1/m` 约定不一致是同一类错误 —— **是我的问题，不是他的。**

**下次开场的正确做法：**
1. 先问清楚他卡在哪一层：是下标平移？是 `g_t = λ·w_t` 这一步？还是整件事的目的？
2. **统一回他熟悉的下标** `v_t = β v_{t−1} + g_t`，一次只改一个地方
3. 先单独确认 `g = λw`（对 `L=½λw²` 求导；他已接受 λ 是二阶导，这步应该很快）
4. 再做消元。目标形状已经给过他了

**这个递推的答案（供批改）：** `w_{t+1} = (1+β−lr·λ) w_t − β w_{t−1}`。
β=0 时退化成 `(1−lr·λ)w_t`，与他自己推过的一致 —— **已把这个自检点预先告诉他了。**

之后要教的新工具：设 `w_t = z^t` 得特征方程 `z² − (1+β−lrλ)z + β = 0`，
两根都在单位圆内 ⇔ 收敛。三个条件里 `1+p+q>0` 那条给出 `lr·λ < 2(1+β)`。

### 五、停在哪（本次未动的东西）

**RMSProp 的三道题已经布置但完全没做**（他先问了最优 lr，然后就卡住了）：

```
s_t = β s_{t−1} + (1−β) g_t²          注意这里必须带 (1−β)：要的是「典型大小」不是「累加量」
w ← w − lr · g/√s
```

- Q1 两个方向的 `s`、`√s` 各约多少？（用梯度表：陡 20~6.5，平 0.2）
- Q2 两个方向的 `g/√s` 各约多少？（期望答案：都约等于 ±1）
- Q3 实际步长还差 100 倍吗？**κ 还重要吗？**

**优先级：先清卡点，再做这三题。** 之后 → Adam → 学习率调度 → mini-batch → `optim_compare.py`。

---

## 2026-09-15 — M2 第二课（下）：E9 清除 + 路线问题（CNN/RNN）

**设备：笔记本**（无 Python，全程纸笔推导，正合适）。

### 一、开场的路线问题：CNN / RNN 要不要学

他主动问的，是个好问题。给的判断：

| | 结论 | 投入 |
|---|---|---|
| RNN / LSTM | 学，但**不是为了用它** | 约一周（大纲 M5 已含） |
| CNN | **不单独学**，只吸收三个概念 | 约半天，并进 M6 |

**RNN 保留的四条理由**（按重要性）：
1. 不亲自撞一次 seq2seq 的信息瓶颈，注意力就是凭空冒出来的公式 —— **头号理由**
2. BPTT = 他刚练熟的反向传播 + 权重共享，是 L0 多路径求和规则的真实用武之地，成本低
3. 🌟 **RNN 思想在 2026 是活的**：Mamba / SSM / 线性注意力 / RWKV 全是「递归形式 ↔ 并行形式」两面。这是他可能切进去的口子
4. KV cache 本质是「让 Transformer 推理时表现得像 RNN」

**明确告诉他不要做的：** 别花两周调 LSTM 超参。写个 char-level RNN，看到梯度消失，搞懂门在拦什么，理解瓶颈，走人。

**CNN 保留的三个概念**（其余是历史）：残差连接（ResNet→每个 Transformer block）；
BatchNorm vs LayerNorm 的**对比本身**（为何 NLP 不能用 BN：变长 + 推理时依赖 batch 统计）；
**卷积 = 权重共享 + 局部性 = 归纳偏置** —— 用来反衬 Transformer 几乎不带归纳偏置 → 更通用也更吃数据 → 接缩放定律。
**改变判断的条件已说明：** 要做多模态/VLM 就补 ViT patch embedding 和音频前端卷积，到时再补。

**顺带给了一条判断标准（这是本次的附加收获）：**
> 判断一门老技术要不要学，不问它历史上多重要，问两件事：
> (a) 它是不是理解当前某个东西的**前提**？(b) 它的**思想**现在还活着吗？
> RNN 两条都占；CNN 对 NLP 只占 (a) 的一小块 → 降级成三个概念，不是一个模块。

与 09-09「判断一个方法重不重要，先问它在什么尺度上起作用」是同一类元判断。

### 二、⚠️ E9 的真实根因 —— 完全是我的问题，且比预估的更基础

我上次的猜测是「下标平移把他绊倒了」。**实际更严重。** 他的原话：

> 「卡住是不知道 g 是什么，也不知道 `g = λ·w`，v 也不是很确定，就是一下子没搞清楚这些符号代表的是什么。还有下标的意义也不是很明白。」

**我一口气引入了 `g`、`λ`、新下标三样东西，一个都没定义。** 他以为 `g` 是个新概念，
其实 `g` 只是 `dL/dw` 的缩写。**这是纯粹的记号事故，不是理解力问题。**

**有效的修复手法（全部是要诀 3「代具体数字」）：**

1. **`g = λw` 不是新公式，就是他盯了两天那张表的两列关系。** 直接把 09-10 的表拿出来乘一遍：
   ```
   20 × ( 1.0000) =  20.0000 ✓      0.2 × (1.0000) = 0.2000 ✓
   20 × (-0.8000) = -16.0000 ✓      0.2 × (0.9820) = 0.1964 ✓
   20 × ( 0.6400) =  12.8000 ✓      0.2 × (0.9643) = 0.1929 ✓
   ```
   每一行都对得上 —— 因为 `dL/dw₁ = 20w₁`，而 20 就是 λ。**一看就通。**

2. **下标的意思 = 他 A 题已经做过的事。** 他写过 `v₁=g, v₂=βv₁+g, v₃=βv₂+g`，
   那就是 `v_t = β v_{t−1} + g_t` 在 t=2,3 的实例。
   **他早就会了，只是没把具体编号和字母下标认成同一件事。**

3. 符号表列成表格（`w`/`g`/`λ`/`v`/`t` 五行），每行配「在你的例子里是谁」。

### 三、消元结果

**第一次的答案：** `w_t = (β + 1 + λ·lr) w_{t−1} + β w_{t−2}`
→ **结构完全正确**（项数、位置、哪项配 β 全对），**两个符号都反了**。

结构对说明消元本身他根本没问题，**再次印证卡点纯粹在符号**。

⚠️ **但自检又没跑。** 我上次已经预先把自检点给了他（β=0 必须退化成 `(1−lr·λ)w_{t−1}`）。
他的错误答案在 β=0 时给出 `(1 + lr·λ)w_{t−1}` —— **公比大于 1，梯度下降却让 |w| 越跑越远**，
一眼可见。**这是薄弱点第 2 条（有判据但不主动用）的第三次出现。**已当场指出，并说明
「写完式子的下一秒先代极端值验一下，比重推一遍便宜得多」要变成肌肉记忆。

**订正后：** `w_t = (1 + β − lr·λ) w_{t−1} − β w_{t−2}` ✅ **E9 清除。**

（注：我说了「两个符号都反了」，所以他这次的订正有可能是被提示翻过去的，
不一定是自检跑出来的。**已要求他下次开场回答「β=0 代进去变成什么」来确认。**）

### 四、已布置未收：特征方程三步（新工具）

**教法用了要诀 2「从他已有的结论做最小改动」：** 单项递推 `w_t = c·w_{t−1}` 的解是指数 `cᵗ`
→ **那就猜两项递推的解也是指数**，设 `w_t = zᵗ` 代进去看 z 要满足什么。
不是新魔法，是拿已知的解的形状去试更难的情形。

- **任务 1：** 代 `w_t = zᵗ` 进 `w_t = p·w_{t−1} + q·w_{t−2}`，两边除以 `z^{t−2}`（t 就消失了），
  得特征方程。答案：`z² − (1 + β − lr·λ) z + β = 0`
- **任务 2（自检）：** 令 β=0 应可因式分解，两根之一正是他自己推过的 `c = 1 − lr·λ`。
  **新工具没推翻旧结论，只是把它包含进去了** —— 已告诉他这是判断新工具靠不靠谱的标准动作
- **任务 3 前的引子（他要先答这个）：** 把 `f(z)` 看成开口向上的抛物线，两根都在 `(−1,1)` 内
  ⇔ `f(1)>0` 且 `f(−1)>0`。问他：**上次 lr 太大时 `c₁ = −9.00`，是从 +1 还是 −1 那侧跑出去的？**
  → 答对就知道该算 `f(−1)`，算出来令其 >0 直接得到 `lr·λ < 2(1+β)`
- **`f(−1) = 2 + 2β − lr·λ`**（供批改）。这个上界上次是我直接甩给他的，**这次由他自己推出来**

理论上界 `2(1+β)/λmax`（λmax=20）对照上次实测：β=0→0.1000/0.0995，0.5→0.15/0.1495，
0.9→0.19/0.1895，0.99→0.199/0.1985。**吻合极好，可以作为收尾的说服力一击。**

### 五、他提的两条元反馈（都已落实）

1. **「符号含义要不要记录一下，或者有啥办法尽量保证教学中符号一致」**
   → 🌟 **他自己提出要解决我的问题，这个意识值得记一笔。** 已建 [`NOTATION.md`](../NOTATION.md)
   作为单一事实来源，并在 `CLAUDE.md` 里立了硬规则：**讲任何带符号的内容前先 Read 它**。
   表里还预先登记了未来的冲突（`z` 的两义、`β` 在 Adam 里要拆成 `β₁β₂`、
   `λ` 撞 L2 正则系数时改用 `α`、`(1−β)` 动量不带 RMSProp 带）。

2. **「行内公式没有按照 latex 风格渲染，看起来还是有点怪怪的」**
   → 反引号包住单个变量会渲染成灰色等宽代码块，夹在中文里确实难看。
   **已改约定：行内单个符号直接写 Unicode 裸字符**（w、β、λ、κ、σ），
   反引号只留给多符号表达式和代码标识符。`$$` 独立成行不变。
   **并在本次末尾埋了一个三选项探针**，让他下次开场报告哪种能渲染 —— 到时按结果定死。

**渲染探针第一轮结果（2026-09-15 当场反馈）：**
A（裸 Unicode）「稍微好点」／ B（反引号）「很难受，**反引号就给代码块比较好**」／ C（`$...$`）**渲染不出**。
**他明确要求行内改用 `\(...\)`。** 09-09 的实测结论是 `\(...\)` 只在内容含反斜杠命令时才渲染
（`\beta` 有效，裸 `\(w\)` 会挂），但那次没系统测过 —— **已埋 D1–D5 五项探针复测**：
`\(\beta\)` ／ `\(w\)` ／ `\(w_t\)` ／ `\(lr \cdot \lambda < 2(1+\beta)\)` ／ `\(\mathit{w}\)`。
D5 是关键：若 D2 挂而 D5 通，说明「塞一个反斜杠命令进去」是通用 workaround。
**下次开场先问结果，再把规则 11 定死。**

**渲染探针第二轮（2026-09-18）—— 规则已定死：**
D1 `\(\beta\)` ✅／D2 `\(w\)` ❌ 显示成 `(w)`／D3 `\(w_t\)` ❌／D4 完整表达式 ✅／D5 `\(\mathit{w}\)` ✅
E1 `\(\mathit{w}_t\)` ✅／E2 `\(\mathit{a} = \mathit{b} + \mathit{c}\)` ✅／**E3 `\(x^2 + 1\)` ✅**

E3 通过推翻了「必须含反斜杠命令」的假设。**真正的判据是「内容看起来像不像数学」** ——
光秃秃一个标识符（`w`、`w_t`）会被当成普通括号，含运算符或反斜杠命令的就当数学渲染。
**结论：行内一律 `\(...\)`，裸拉丁变量用 `\mathit{}` 包。** 已写进 `CLAUDE.md` 规则 11 与 `NOTATION.md` 第五节。

**渲染探针第三轮（2026-09-18）—— 规则最终定死，两个独立条件：**

第二轮定的规则当场被推翻：我写「`\(\mathit{w}\)`、`\(\mathit{w}_t\)`、`\(\mathit{g}\)`」时只有第一个渲染。
F1–F4 + 复查上一条消息，共 8 个位置：

| | `\(` 前面是 | 结果 |
|---|---|---|
| F1 / F2 第1个 | `：` | ❌ |
| F2 第2、3个 | `、` | ❌ |
| F3 三个 / F4 | 空格（含行首） | ✅ |
| 上条第1行前半 / 第2行两个 | 空格 | ✅ |
| 上条第1行后半 | `，` | ❌ |

**8/8 无例外：`\(` 前面是空白就渲染，是中文标点就挂。** G1/G2（中文标点后补空格）复验通过。
结束符 `\)` 后面接 `，。、` 无影响。加上第二轮的发现，共两个独立条件，已写进规则 11 与 `NOTATION.md`。

⚠️ **教学自省：我第二轮拿 3 个样本就把规则「定死」，当场被推翻。**
这正是我一直在要求他做的事（先问结论在什么范围内成立），已当他的面承认。
**这类「我自己踩了我教的坑」的实例对他有说服力，值得记下来。**

## 2026-09-18 — M2 第二课（下）续：特征方程全链路，动量的真正机制

**设备：笔记本。** 前半段花在定死公式渲染规则（三轮探针，见上），后半段全是推导。

### 一、E10 自检：问了就做对

Q1「β=0 代进 `w_t=(1+β−lr·λ)w_{t−1}−βw_{t−2}` 变成什么」→ `w_t = (1 − lr·λ) w_{t−1}` ✅

**但是是我开口问了他才做的，还不是自发的。** E10 保持开放。

### 二、特征方程三步 —— 全部独立完成

| 步 | 他的答案 | 判定 |
|---|---|---|
| 特征方程 | `z² + (−1−β+lr·λ)z + β = 0` | ✅（与 `z²−(1+β−lrλ)z+β=0` 同式） |
| 哪侧跑出去 | 「c₁=−9 是不是就是…的一个根，那么应该就是 f(−1)>0」 | ✅ **自己把一周前的 c 和今天的根接上了** |
| 算 f(−1) | `1 + 1 + β − lrλ + β > 0`，移项 | ✅ 第一次只说「推出来就是」，被要求才写 |

**`lr·λ < 2(1+β)` 这次是他自己推出来的**（09-13 是我直接甩给他的）。
对照实测：四个 β 值全部**恰好差 0.0005 = 扫描步长** —— 顺势教了「看残差有没有结构」：
随机 → 噪声；常数 → 系统性因素；随变量变 → 模型漏了一项。

### 三、⚠️ 我的误诊（要诀 7 需要修正）

他写「c₁=−9 **是不是就是**…」，我当场按要诀 7 点了「信心问题第四次」。
**他纠正：不确定是因为记不清 lr 和 λ 的含义了。** —— 那是记号遗忘，不是信心问题。

**教训：点信心问题之前，先问一句「哪里不确定」。** 问句有两种来源：
判断对了但不敢认（信心），和前提记不清所以不敢认（记号）。处理方式完全不同。
→ 已告诉他 **`NOTATION.md` 是给他用的，不只是约束我**。之前没说，是我的疏忽。

### 四、🌟 他抓到了我表述里的一个真漏洞

我写「动量有效步长放大 `1/(1−β)=10` 倍」和「理论 `O(√κ)`，κ=100 时 10 倍」放在一段里。
**他问：κ=1000 时 `1/(1−0.9)` 还是 10，但 `√1000≈31.6`，那凭什么说是根号？**

**完全正确 —— 两个 10 是巧合。** 回答要点：
- `O(√κ)` 要求 **β 随 κ 调到最优**：`β* = ((√κ−1)/(√κ+1))²`，κ=100/1000/10⁴/10⁶ → 0.669/0.881/0.961/0.996
- 代入后 `1/(1−β*) ≈ √κ/4` —— 他的「放大倍数」直觉本身没错，**前提是 β 跟着 κ 走**
- 步数：plain GD `≈(κ/2)ln(1/ε)`，最优动量 `≈(√κ/2)ln(1/ε)`，比值 √κ

**这是 09-13 之后第二次他问出我讲解里的问题**（第一次是「什么叫最优 lr」）。要诀 8 再次验证。

### 五、「为什么是根号」—— 从他自己的特征方程推出来

| 问 | 他的答案 | 判定 |
|---|---|---|
| 韦达：两根之积 | β | ✅ |
| 共轭复根时 `|z|` | `z₁z₂=β → |z₁||z₂|=β → |z|²=β → |z|=√β` | ✅ 链条一步没跳 |
| 还有 λ 吗 | 没有了 | ✅ |

**核心结论（他亲手拿到）：复根区内所有方向都以同一个 √β 收敛，κ 被抹平。**
这比「动量减少震荡」深一层，是真正的机制。

**他追问「为什么 z 的模长决定收敛速度，z 和 w 什么关系」**（好问题）→ 讲了：
单项 `w_t=cᵗw₀` → 线性叠加 `w_t=Az₁ᵗ+Bz₂ᵗ` → 复数 = 缩放 |z| + 旋转 θ，旋转不改大小
→ `w_t = C(√β)ᵗcos(tθ+φ)`，**复根 = 衰减振荡**。
对照他老表里 w₁ 正负交替 = 负实根 = 每步转 180°。

**他又问「包络是什么」** → 敲钟比喻 + √β=0.9、θ=90° 的数字表。表里 w 每隔一步过零，
而包络还有 0.9 —— **顺势说明了「第一次 |w|<ε」作为停止判据的陷阱**（见第七节）。

### 六、复根区 → κ 上限（做到第 2 步，暂停）

| 步 | 他的答案 | 判定 |
|---|---|---|
| 解判别式 | `(√β−1)² < lr·λ < (√β+1)²`，**完全平方自己凑的** | ✅ |
| 两端条件 | `λmin > (1−√β)²/lr`，`λmax < (1+√β)²/lr` | ✅ |
| 消 lr | `κ < (1+√β)²/(1−√β)²` | ✅ **走了自己的路**：我提示求 lr 的上下界，他直接解 λ 再相除，更短 |

**停在这里，第 3–5 步未做：**
- 第 3 步：由 `√κ < (1+√β)/(1−√β)` 解出 `√β > (√κ−1)/(√κ+1)`
- 第 4 步：包络是 √β → β 越小越快；但必须 ≥ 下限才装得下所有方向 → **最优卡在边界**，`β* = ((√κ−1)/(√κ+1))²`
  （他已经见过「两股反向力 → 最优在边界」的结构，是最优 lr 那次）
- 第 5 步：κ=1000 → `√κ=31.62`，`β*=(30.62/32.62)²≈0.881`，**要求他先预测比 0.9 大还是小**（答：小）
- 推完可以告诉他：这是 **Polyak 重球法（heavy ball, 1964）** 的最优参数 —— 给名字方便他以后检索

### 七、⚠️ 我自己的待办：71 步那个实测数可能有问题

按 √β 算，β=0.9 时包络缩到 1e-3 约需 `ln(1000)/(−ln√0.9) ≈ 131` 步。我 09-13 报的实测是 **71 步**。
检查过 β=0.9、lr=0.0395 时两个方向（lrλ=0.79 和 0.0079）**都在复根区**，所以理论上两个方向都该按 √0.9 走。
β=0.99 同样：理论 ≈1379 步，报的实测 430 步。**实测系统性地比理论快，不合理。**

**怀疑：当时停止判据是「第一次 max|w| < 1e-3」，被振荡的过零点提前触发了。**
而且扫最优 lr 时，网格搜索会**主动挑出**过零点恰好对齐的 lr —— 优化器在利用测量漏洞。

**回台式机必须复核**：改用「此后 N 步都保持 < ε」或直接拟合包络。
若确认，则 09-13 的「实测 4.9 倍」以及「β=0.99 反而慢（430 步）」都要重算。
**已当他的面说了这个怀疑。复核结果要如实告诉他，这本身就是「拿理论核对读数」的最好教材。**

### 八、附带：全局配置

应他要求建了 `~/.claude/CLAUDE.md`（排版规则，所有项目生效）和 `~/.claude/skills/math-rendering/`
（换客户端后的探测方法）。**讲清了为什么排版规则不能只放 skill**：skill 按需触发，排版是每次都要遵守的。
**这两个文件只在笔记本上** —— 已备份到仓库 `dotclaude/`，台式机首次会话时要装。

## 2026-09-20 — β* 收尾 + ⚠️ 复核推翻了我 09-13 的实测数据

**设备：笔记本。环境有变：他自己装好了 Python 3.13.15（`D:\Program\Python\Python313`），
项目内 `.venv` 已建但只有 pip —— 本次会话补装了 numpy 2.5.3 / scipy / matplotlib / scikit-learn。**
→ **2D 优化实验不再需要台式机，笔记本就能跑。** 这直接让我提前复核了 71 步那个疑点。

### 一、β* 推导第 3–5 步

| 步 | 他的答案 | 判定 |
|---|---|---|
| 3 | `√β > (√κ−1)/(√κ+1)` | ✅ |
| 4 | 「单看收敛速度越小越好，所以取到这个下限大一点点」 | ✅ **还答准了严格不等式 → 下限取不到，只能逼近** |
| 5 预测 | 「κ=1000 时 β* 应该比 0.9 大吧」 | ❌ 实为 **0.8811，比 0.9 小** |

**预测错的根因（已讲）：他把 0.9 当成「中等」设置。** 反查表纠正这个直觉：

```
beta   它最多能摆平的 kappa
0.5             34
0.669           99.7
0.881          997
0.9           1442      <- 0.9 其实已经很强
0.99        158402
```

### 二、⚠️ 复核结果：09-13 报的「71 步」是测量假象，我错了

同一个 2D 问题（λ=20/0.2，κ=100，w₀=(1,1)，tol=1e-3），两种停止判据对比：

| β | A：首次 max\|w\|<tol | B：连续 50 步 <tol | 理论 `ln(1000)/(−ln√β)` |
|---|---|---|---|
| 0 | 346 | 346 | — |
| 0.5 | 107 | 107 | (不适用) |
| **0.9** | **71** | **129** | **131** |
| 0.99 | 430 | 1354 | 1375 |

**判据 A 被振荡过零点提前触发**，β=0 和 0.5 不受影响（实根、不振荡），只有复根区的 β 中招。
判据 B 与理论吻合到 1.5% 以内。

**🔥 最严重的后果：这个假象把一个比较关系搞反了。**
- 旧（错）数据：β=0.9（71）**优于** β=0.5（107）
- 真实数据：β=0.9（129）**劣于** β=0.5（107）
> **一个坏判据不只是让数字偏大偏小，它能让你得出相反的结论。**
> 已当他的面讲：这就是为什么论文里「怎么测的」和「测出什么」同等重要。

### 三、🌟 他推的 β* 公式，预测对了实验（本次最大的收获）

β 从 0 到 0.98 全扫，每个 β 各自网格搜最优 lr，用判据 B：

```
empirical best : beta=0.68   steps=42   lr=0.1630
theory  beta*  : 0.6694                        <- 差一格网格（网格步长 0.02）
plain GD       : 346 steps  ->  speedup 8.24x  （理论 sqrt(kappa)=10）
```

- **β ≥ 0.70 时实测与 √β 理论几乎重合**：0.72→44/42，0.80→61/62，0.90→129/131，0.98→674/684
- **β < 0.68 时理论列完全对不上**（β=0.5 理论 20、实测 107）—— 因为 √β 的结论只在**复根区**成立，
  而 β<β* 时 λmin 那个方向掉出复根区变成实根。**这正是他第 4 步推的「最优卡在边界」的实验证据。**
- **已把「为什么 β<0.68 时理论列是废的」留给他回答**，没有直接讲

**修正后的诚实结论：** 动量在 κ=100 上的真实加速是 **8.24 倍**（理论 10 倍），
不是我 09-13 说的 4.9 倍 —— 那个数既用了坏判据，又用了非最优的 β=0.9。
**剩下的 1.8 倍差距来自瞬态和离散步长，是真实的、可解释的。**

### 四、待办变更

- ✅ 「回台式机复核 71 步」**已在笔记本完成，撤销该待办**
- 📌 09-13 会话日志里的「4.9 倍」「β=0.99 反而慢 430 步」已被本次推翻，
  历史记录保留原样（那是当时的真实判断），修正见本节
- 错题 E6 / E8 今天到复查期，本次会话已提出，**尚未考**

**排版补丁（2026-09-20）：** 他发现我一张表格渲染错位。**根因：我在表格单元格里写了绝对值 `|w|`，
裸竖线被 Markdown 当成列分隔符**，那一格被劈成三格、表头 6 列而分隔行 4 列。
→ 已定第三条规则：**表格里绝对值写 `\lvert ... \rvert`，纯文本转义成 `max\|w\|`**。
已同步到 `CLAUDE.md` 规则 11、`NOTATION.md` 第五节、全局 `~/.claude/CLAUDE.md` 及 `dotclaude/` 备份。

顺带跟他讲了「—」与「（不适用）」的区别：前者是**式子算不出来**（`ln 0` 无定义），
后者是**式子算得出来但前提不成立**（β=0.5 不在复根区，那个 20 是无意义的数）。
> **第二种更危险：公式在前提不成立时照样吐数字，不报错。** 已埋回给他那道未答的题。

⚠️ **档案体积：`CLAUDE.md` 15.9KB，超软上限 15KB 约 4%。** 已压过两轮（16.6→15.9）。
**M2 结业时把第三节那段压成表格一行，会一次性腾出空间**，届时再处理，不要为了几百字节反复折腾。

## 2026-09-20（续）— 追查「理论为什么失效」：他问出了最关键的一问

### 一、他的四个数全对

代入复根条件 `(1−√β)² < lr·λ < (1+√β)²`，平方向 `λmin=0.2`：

| β | 最优 lr | `lr·λmin` | 下界 `(1−√β)²` | 在区间里？ |
|---|---|---|---|---|
| 0.5 | 0.1475 | 0.0295 | 0.08579 | ❌ 不在 |
| 0.9 | 0.0150 | 0.0030 | 0.00263 | ✅ 在（勉强） |

**注意 β=0.9 那行只超出下界 14% —— 它离掉出复根区非常近。** 这正好对应 β=0.9 远大于 β*=0.669。

### 二、🌟 他问了三个问题，第三个是整段里最好的一问

> 「**那万一是因为 lr 太小了导致掉出了下界呢？**」

**他没有接受「掉出区间」这个现象，而是去质疑造成它的原因是否可以消除。** 这是该有的反应 ——
等价于在问「这是本质限制还是实验设置的偶然」。已当面肯定。

另外两问「为什么这样算 / 不在区间里的意义」「最优 lr 为什么是这两个值」也都答了：
- **掉出区间 = 两根变实根。** 韦达仍给 `z₁z₂=β`，但两根不再相等 → **一个大于 √β 一个小于**；
  `w_t = Az₁ᵗ + Bz₂ᵗ` 里**慢的那项说了算**，所以实际收敛率是大根，比 √β 慢。
  **→ 「√β 公式算出的 20 步」是个无效数字，不是理论错。**
- 最优 lr 是网格暴力扫的（0.0005 步长），**每个 β 各自取最优** —— 回指 09-13 教的公平比较原则。

### 三、已布置未收：两道题（答案备查，别提前说）

**任务 1** —— β=0.5、λmax=20、λmin=0.2，把复根条件两头都解成 lr 的范围：

```
平方向不掉下界：lr > (1−√β)²/λmin = 0.085786/0.2  = 0.42893
陡方向不冲上界：lr < (1+√β)²/λmax = 2.914214/20   = 0.14571
                        0.42893 > 0.14571  ->  空集，无解
```

**→ 不是 lr 挑小了，是根本不存在能同时满足两头的 lr。**
**这和他自己推的 `κ < (1+√β)²/(1−√β)²` 是同一件事**：β=0.5 的 κ_max = **33.97**，而问题是 κ=100。
**收口时要把这两种说法接起来 —— 同一个事实的两种写法。**

**任务 2** —— β=0.5、lr=0.1475、λ=0.2 的特征方程 `z² − 1.4705z + 0.5 = 0`：

```
判别式 = 1.4705² − 2 = 0.16237,  sqrt = 0.402952
z_max  = (1.4705 + 0.402952)/2 = 0.936726     ( > sqrt(0.5)=0.7071 ✓ )
步数   = ln(1000)/(−ln 0.936726) = 105.7  ->  106      实测 107
```

**差一步。** 结论要明确讲给他：**理论没错，是我套错了公式** —— 复根区用 √β，实根区要用大根。
「理论失效」这个说法本身是错的。

**顺带可讲（已核算，可选）：** 该 lr 下陡方向其实也掉出了复根区（`lr·λmax=2.95 > 2.9142`），
两根 −0.5649 / −0.8851，需 ~57 步。**两个方向取最大 → 106 步由平方向决定**，与实测 107 吻合。

### 四、本次未做

- E6 / E8 复查（到期 2026-09-20）**仍未考**
- RMSProp 三题**仍未做**
