# 符号约定表 —— 单一事实来源

> **⚠️ Claude：讲任何带数学符号的内容之前，先读本文件。引入新符号必须先写进这里。**
> 立本文件的原因：2026-09-13 学习者卡在动量消元上整整一次会话，
> 事后查明**根因是 Claude 自己下标写法前后不一致**（`v_t=βv_{t−1}+g_t` → `v_{t+1}=βv_t+λw_t`，
> 同时换了下标和变量）。这不是第一次 —— M1 的 `1/m` 约定也坑过一次。
> **记号不一致是本项目目前造成学习阻塞的头号原因，且责任全在 Claude 一方。**

---

## 使用规则（四条，必须遵守）

1. **引入任何新符号前先查本表。** 表里有就照用，表里没有就先加进来再用。
2. **一次只改一个地方。** 不要同时换下标体系又换变量含义 —— 这正是 E9 的成因。
3. **下标约定一旦定了就不再改。** 本项目统一用 `v_t = β v_{t−1} + g_t` 这种「当前步在左」的写法。
4. **符号有冲突时必须当场点明**（见下面的「已知冲突」一节），不能指望他自己分辨。

---

## 一、基础记号（L0 / M1 建立）

| 符号 | 含义 | 备注 |
|---|---|---|
| `x` / `X` | 输入 | `X` 形状 `(n_x, m)`，**样本在列**（Andrew Ng 约定） |
| `y` / `Y` | 标签 | |
| `m` | 样本数 / batch 大小 | **样本数一律用 `m`，不用 `n`** |
| `n_x` / `n_h` / `n_y` | 输入维 / 隐层单元数 / 输出维 | |
| `W1, b1, W2, b2` | 参数，数字后缀 = 层号 | `W1` 形状 `(n_h, n_x)` |
| `z` / `Z` | 线性输出 `z = Wx + b` | ⚠️ 见「已知冲突」 |
| `a` / `A` | 激活输出 `a = σ(z)` 或 `relu(z)` | |
| `L` | 单样本损失 | |
| `J` / cost | 平均损失，**含 `1/m`** | |
| `σ` | sigmoid | |
| `dL/dw` | 梯度的书写方式 | **约定写 `dL/dw`，不写 `L/w`**（后者字面是除法）；`∂` 难打 |
| `dW1, db1, dZ1, dA1` | 代码里的梯度变量 | `dX` 恒指 `dJ/dX`，**已含 `1/m`**（M1 踩过这个坑） |
| `rng` | `np.random.default_rng(seed)` | 不用老 API `np.random.seed()` |

## 二、初始化（M2 第一课）

| 符号 | 含义 | 备注 |
|---|---|---|
| `n` | **该层自己的 fan-in** | 不是样本数。`W1` 用 `n_x`，`W2` 用 `n_h` |
| `Var(w)` | 权重方差 | Xavier `1/n`，He `2/n` |
| `E[a²]` | 激活的二阶矩 | `E[a²] = ½E[z²]`（只需分布对称） |
| `k` | 逐层能量放大倍率 `E[Z2²]/E[Z1²]` | `k = n_h·Var(w2)/2`；灾难在 `k^L` 的指数上 |

## 三、优化器（M2 第二课）

| 符号 | 含义 | 备注 |
|---|---|---|
| `lr` | 学习率 | |
| `g` | **梯度，就是 `dL/dw` 的缩写** | **没有任何别的含义。**（2026-09-15 他就卡在这里） |
| `λ` | 曲率 = 二阶导 | `L = ½λw²` ⟹ `g = λw`；多维时是 Hessian 特征值 |
| | | 他的例子 `L = 10w₁² + 0.1w₂²` → **λ₁ = 20，λ₂ = 0.2** |
| `λmax` / `λmin` | 最大 / 最小曲率 | |
| `κ` | 条件数 `λmax/λmin` | 他的例子 κ = 100 |
| `c` | 普通 GD 的单步公比 `c = 1 − lr·λ` | 收敛 ⇔ `|c| < 1` |
| `β` | 动量衰减系数 | ⚠️ 见「已知冲突」 |
| `v` | 动量累积值（速度） | `v_t = β v_{t−1} + g_t`（PyTorch 约定，**不带 `(1−β)`**） |
| `s` | RMSProp 的梯度平方滑动平均 | `s_t = β s_{t−1} + (1−β) g_t²`（**带 `(1−β)`**） |
| 下标 `t` | 第几步 | `v_t = β v_{t−1} + g_t` 念作「这一步的 v = β × 上一步的 v + 这一步的梯度」 |

**动量的两项递推（2026-09-15 他独立推出）：**

`w_t = (1 + β − lr·λ) w_{t−1} − β w_{t−2}`　　（β=0 时退化成 `w_t = (1 − lr·λ) w_{t−1}`）

**特征方程：** 设 `w_t = zᵗ` ⟹ `z² − (1 + β − lr·λ) z + β = 0`，两根都在单位圆内 ⇔ 收敛。

---

## 四、⚠️ 已知冲突（必须当场点明，不能指望他自己分辨）

| 符号 | 冲突 | 处理 |
|---|---|---|
| `z` | M1：线性输出 `z = Wx+b` ／ M2：特征方程的根 `w_t = zᵗ` | **完全无关的两个东西**，用到时必须明说 |
| `β` | 动量的 β ／ RMSProp 的 β ／ Adam 的 `β₁ β₂` | Adam 出场时统一改用 `β₁`（动量）、`β₂`（RMSProp） |
| `λ` | 曲率 ／ L2 正则系数（M2 后半会撞上） | 讲正则化时改用 **`α`** 表示正则强度，`λ` 保留给曲率 |
| `n` | fan-in ／ 样本数 | 样本数一律 `m`，`n` 只表示单元数 |
| `(1−β)` | 动量**不带**，RMSProp **带** | 讲 RMSProp 时必须明确对比，说明「要的是典型大小不是累加量」 |
| `1/m` | 梯度里带不带 | 本项目：`dW` 等代码变量**一律已含** `1/m` |

---

## 五、排版约定（2026-09-18 实测定死，对应 `CLAUDE.md` 规则 11）

- **重要公式** → `$$...$$` 独立成行
- **行内数学** → `\(...\)`
- ⚠️ **裸拉丁变量必须用 `\mathit{}` 包**：`\(\mathit{w}\)`、`\(\mathit{w}_t\)`、`\(\mathit{g}\)`。
  否则 `\(...\)` 会退化成普通括号（`\(w\)` → `(w)`）—— 渲染器要求内容「看起来像数学」才接管。
  希腊字母 `\(\beta\)`、带运算符的式子 `\(x^2 + 1\)` 直接写即可。
- ❌ **反引号不包数学**，只用于代码标识符：`np.sum`、`dW1`
- ❌ `$...$`、`\[...\]` 不渲染
